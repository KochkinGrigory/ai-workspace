---
name: telegram-notifier
description: Отправка уведомлений, отчётов и файлов пользователю через Telegram бота. Использовать когда пользователь явно просит отправить что-то в Telegram, уведомить через бота, или когда сессия Claude Code запущена из Telegram и нужно отправить результаты. Триггеры 'отправь в телеграм', 'пришли в тг', 'скинь в бота', 'уведоми в telegram', 'send to telegram', 'notify telegram', 'пришли результаты в бота', 'скинь отчёт в тг', 'отправь файл пользователю'.
version: 1.0.0
author: AI Workspace
license: Proprietary
tags: [telegram, notifications, messaging, files]
---

# Telegram Notifier Skill

Этот skill позволяет Claude отправлять сообщения, уведомления и файлы пользователю через Telegram бота.

## 🚀 Быстрый старт для Claude

Когда пользователь просит отправить что-то в Telegram, используй **прямой вызов shell скрипта**:

**Отправить текст:**
```bash
/opt/ai-workspace/commands/tg-send.sh "сообщение с HTML форматированием"
```

**Отправить файл:**
```bash
/opt/ai-workspace/commands/tg-send.sh --file /path/to/file.xlsx "описание файла"
```

**⚠️ ВАЖНО:** Этот skill для **разовых отправок** из обычного Claude Code сеанса.

Если ты запущен **из Telegram бота** (видишь файл `telegram_agent_context.md` в контексте) - используй команды `/update`, `/tg`, `/tg-file` описанные там, а НЕ этот skill.

---

## 🎯 Когда использовать этот skill

### ОБЯЗАТЕЛЬНО используй, когда:
1. **Пользователь явно просит** отправить что-то в Telegram
   - "Отправь мне в телеграм"
   - "Пришли результаты в бота"
   - "Уведоми меня в Telegram когда закончишь"
   - "Скинь в тг"
   - "Отправь ... в бота"

2. **Завершение длительных операций** (по запросу пользователя)
   - Анализ большого объёма данных завершён
   - Отчёт готов
   - Миграция/деплой завершён

3. **Отправка файлов** (по запросу)
   - Excel отчёты
   - PDF документы
   - Графики и визуализации
   - SQL дампы
   - Логи

4. **Критические события** (если пользователь настроил)
   - Ошибки в production
   - Превышение порогов метрик
   - Важные системные события

### НЕ используй автоматически для:
- ❌ Обычных ответов на вопросы (отвечай в консоль)
- ❌ Промежуточных результатов (если не попросили)
- ❌ Подтверждений выполнения простых команд
- ❌ Дублирования всех сообщений (только по запросу)

## 📋 Возможности

### 1. Отправка текстовых сообщений

**Простой текст:**
```bash
/opt/ai-workspace/commands/tg-send.sh "Анализ завершён!"
```

**HTML форматирование (рекомендуется):**
```bash
/opt/ai-workspace/commands/tg-send.sh "<b>Отчёт готов!</b>

📊 Всего продаж: 1,234
💰 Выручка: 567,890 ₽
📈 Рост: +15.3%"
```

**Markdown форматирование:**
```bash
/opt/ai-workspace/commands/tg-send.sh --markdown "*Важно!* Проверь результаты"
```

### 2. Отправка файлов

**С описанием:**
```bash
/opt/ai-workspace/commands/tg-send.sh --file /path/to/report.xlsx "📊 Отчёт по продажам за октябрь 2024"
```

**Без описания:**
```bash
/opt/ai-workspace/commands/tg-send.sh --file /opt/ai-workspace/.claude/skills/telegram-notifier/tmp/analysis.pdf
```

### 3. Проверка статуса бота

```bash
curl -s http://localhost:8081/health | jq
```

---

## 🔧 Архитектура системы

```
Пользователь: "Отправь результаты в тг"
       ↓
Claude использует этот Skill
       ↓
Вызывает: /opt/ai-workspace/commands/tg-send.sh
       ↓
Shell скрипт → HTTP API (localhost:8081)
       ↓
Python бот (apps/telegram-bot/src/bot.py)
       ↓
Telegram API → Доставка пользователю 📱
```

### Связанные компоненты:

| Компонент | Путь | Назначение |
|-----------|------|-----------|
| **Этот Skill** | `.claude/skills/telegram-notifier/` | Контекст и примеры для Claude |
| **Slash команды** | `.claude/commands/tg*.md` | Альтернативный интерфейс (/tg, /tg-file) |
| **Shell скрипт** | `commands/tg-send.sh` | Исполняемая утилита |
| **Python бот** | `apps/telegram-bot/src/bot.py` | Long polling + HTTP API |
| **Документация** | `apps/telegram-bot/README.md` | Полная документация |

## 🛠️ Технические детали

### HTTP API бота

Бот предоставляет HTTP API на `localhost:8081`:

**POST /send** - отправить текст
```bash
curl -X POST http://localhost:8081/send \
  -H "Content-Type: application/json" \
  -d '{"text": "Hello!", "parse_mode": "HTML"}'
```

**POST /send_file** - отправить файл
```bash
curl -X POST http://localhost:8081/send_file \
  -F "file=@/path/to/file.xlsx" \
  -F "caption=Описание файла"
```

**GET /health** - проверка здоровья
```bash
curl http://localhost:8081/health
```

### Форматирование текста

**HTML теги:**
- `<b>жирный</b>` - жирный текст
- `<i>курсив</i>` - курсив
- `<code>код</code>` - моноширинный шрифт
- `<pre>блок кода</pre>` - блок кода
- `<a href="url">ссылка</a>` - гиперссылка

**Markdown:**
- `*курсив*` - курсив
- `**жирный**` - жирный
- `` `код` `` - моноширинный
- `[текст](url)` - ссылка

### Эмодзи

Используй эмодзи для визуального выделения:
- ✅ Успех, завершение
- ❌ Ошибка, проблема
- ⚠️ Предупреждение
- 📊 Отчёты, аналитика
- 💰 Финансы, деньги
- 📈 Рост, улучшение
- 📉 Падение, ухудшение
- 🔔 Уведомление
- 📁 Файл
- 🚀 Запуск, деплой
- ⏱️ Время, продолжительность
- 🔍 Анализ, поиск

## 📝 Примеры использования

### Пример 1: Уведомление о завершении анализа

```bash
/opt/ai-workspace/commands/tg-send.sh "✅ <b>Анализ CRM завершён</b>

📊 Результаты за октябрь:
• Конверсия: 23.5% (↑2.1%)
• Новых клиентов: 145
• Выручка: 2,345,678 ₽

Полный отчёт отправляю файлом 👇"
```

### Пример 2: Отправка Excel отчёта

```bash
# Сначала уведомление
/opt/ai-workspace/commands/tg-send.sh "📊 <b>Отчёт по продажам готов</b>"

# Потом файл
/opt/ai-workspace/commands/tg-send.sh --file /opt/ai-workspace/.claude/skills/telegram-notifier/tmp/sales_report_oct_2024.xlsx "Детальная разбивка по источникам и товарным категориям"
```

### Пример 3: Уведомление об ошибке

```bash
/opt/ai-workspace/commands/tg-send.sh "⚠️ <b>Внимание!</b>

Обнаружена проблема при миграции БД:
<code>relation 'new_table' already exists</code>

Нужна проверка вручную. Логи во вложении."

/opt/ai-workspace/commands/tg-send.sh --file /var/log/migration_error.log
```

### Пример 4: Статистика по расписанию

```bash
/opt/ai-workspace/commands/tg-send.sh "📈 <b>Ежедневная сводка</b>
🗓️ $(date +'%d.%m.%Y')

<b>Продажи:</b>
💰 Выручка: 89,450 ₽
📦 Заказов: 23
👤 Новых клиентов: 7

<b>Маркетинг:</b>
🔔 Заявок: 45
✅ Конверсия: 51.1%
💸 Расход: 12,300 ₽

Хорошего дня! ☀️"
```

### Пример 5: Прогресс долгой операции

```bash
# Начало
/opt/ai-workspace/commands/tg-send.sh "🚀 Начинаю миграцию 1000 заявок..."

# В процессе (опционально)
/opt/ai-workspace/commands/tg-send.sh "⏱️ Прогресс: 500/1000 (50%)"

# Завершение
/opt/ai-workspace/commands/tg-send.sh "✅ Миграция завершена успешно!
⏱️ Время: 5 мин 23 сек"
```

## 🔍 Диагностика

### Проверка, что бот работает

```bash
# Статус контейнера
docker ps | grep telegram-bot

# Логи бота
docker logs -f ai-workspace-telegram-bot

# Health check
curl -s http://localhost:8081/health | jq

# Тестовое сообщение
/opt/ai-workspace/commands/tg-send.sh "🧪 Тест"
```

### Если бот не отвечает

```bash
# Перезапустить бота
cd /opt/ai-workspace/apps/telegram-bot
docker-compose restart

# Проверить .env
grep TELEGRAM /opt/ai-workspace/.env

# Посмотреть логи ошибок
docker logs ai-workspace-telegram-bot | grep -i error
```

## 🎨 Рекомендации по использованию

### Структура сообщений

**Хорошо:**
```
✅ Заголовок (жирный)

Краткое описание

Детали:
• Пункт 1
• Пункт 2

Действия: [если нужны]
```

**Плохо:**
```
Завершено. Результат: 123. Время: 5 сек. Ошибок нет.
```

### Длина сообщений

- ✅ Короткие уведомления: 1-3 строки
- ✅ Отчёты: 5-15 строк с структурой
- ❌ Длинные логи → отправляй файлом

### Частота отправки

- ✅ Важные события: сразу
- ✅ Отчёты: по запросу или расписанию
- ❌ Спам: избегай частых сообщений без причины

## 📚 Связанные команды

- `docker logs ai-workspace-telegram-bot` - логи бота
- `docker-compose restart` - перезапуск бота
- Helper скрипт: `/opt/ai-workspace/commands/tg-send.sh --help`

## 🔐 Безопасность и очистка

- ✅ Только авторизованный chat_id может писать боту
- ✅ HTTP API доступен только локально (localhost)
- ✅ Токен бота хранится в .env (не в git)
- ✅ Логи ротируются автоматически (30 дней хранения)
- ✅ Временные файлы удаляются после отправки
- ✅ Временная директория: `/opt/ai-workspace/.claude/skills/telegram-notifier/tmp/`
- ✅ **Автоочистка:** Cron задача удаляет файлы старше 7 дней каждый день в 03:00

### 📂 Загруженные файлы от пользователя

Когда пользователь отправляет **фото или документы через Telegram бота**, они автоматически сохраняются на сервере и Claude может их использовать:

**Директории для загрузок:**
- 📸 **Фотографии**: `/opt/ai-workspace/.claude/skills/telegram-notifier/uploads/photos/`
  - Формат имени: `photo_YYYYMMDD_HHMMSS.jpg`
  - Сохраняется лучшее качество

- 📎 **Документы**: `/opt/ai-workspace/.claude/skills/telegram-notifier/uploads/files/`
  - Формат имени: `{original_name}_YYYYMMDD_HHMMSS.{ext}`
  - Сохраняется оригинальное имя + timestamp

**Автоматическая очистка:**
- ✅ Файлы старше **7 дней** автоматически удаляются (cron задача каждую ночь в 3:00)
- ✅ Это предотвращает захламление диска
- ⚠️ Если нужен файл дольше недели - переместите его в другую директорию
- 🔧 Cron настроен через `/etc/cron.d/telegram-uploads-cleanup`
- 📜 Скрипт очистки: `/opt/ai-workspace/.claude/skills/telegram-notifier/cleanup-uploads.sh`

**Как Claude использует загруженные файлы:**

```bash
# Посмотреть последние загрузки
ls -lht /opt/ai-workspace/.claude/skills/telegram-notifier/uploads/photos/ | head -5
ls -lht /opt/ai-workspace/.claude/skills/telegram-notifier/uploads/files/ | head -5

# Прочитать документ
cat /opt/ai-workspace/.claude/skills/telegram-notifier/uploads/files/data_20251103_153045.txt

# Открыть изображение (Read tool может "увидеть" фото)
# Просто используй Read tool с путём к фото

# Обработать Excel файл
# Используй xlsx skill с путём к загруженному файлу
```

**Примеры использования:**

1. **Пользователь отправил фото** → Claude может "увидеть" изображение и дать рекомендации
2. **Пользователь отправил Excel** → Claude может проанализировать через xlsx skill
3. **Пользователь отправил текст** → Claude может прочитать и обработать
4. **Пользователь отправил PDF** → Claude может работать с документом

## 📖 Дополнительная документация

- Полная документация: `/opt/ai-workspace/apps/telegram-bot/README.md`
- Инструкция по настройке: `/opt/ai-workspace/apps/telegram-bot/SETUP.md`
- Управление файлами: `/opt/ai-workspace/.claude/skills/telegram-notifier/references/file-management.md`
- API Reference: `/opt/ai-workspace/.claude/skills/telegram-notifier/references/api-reference.md`
- Formatting Guide: `/opt/ai-workspace/.claude/skills/telegram-notifier/references/formatting-guide.md`
- Исходный код бота: `/opt/ai-workspace/apps/telegram-bot/src/bot.py`

---

## 🎯 Пошаговая инструкция для Claude

### Когда пользователь пишет что-то вроде:
- "Отправь мне в телеграм результаты"
- "Пришли отчёт в бота"
- "Скинь в тг"
- "Уведоми меня когда закончишь"

### Ты должен:

#### Шаг 1: Сформируй сообщение

**Правила форматирования:**
- Используй **HTML** (не Markdown) - это основной формат
- Добавь эмодзи в начало для категории (✅, 📊, ⚠️, и т.д.)
- Используй `<b>заголовок</b>` для выделения
- Используй `<code>значения</code>` для чисел и кода
- Структурируй информацию списками

**Шаблон:**
```html
{эмодзи} <b>{Заголовок}</b>

{Краткое описание}

<b>{Секция}:</b>
• {Пункт 1}: <code>{значение}</code>
• {Пункт 2}: <code>{значение}</code>

<i>{Дополнительная информация}</i>
```

#### Шаг 2: Отправь сообщение

**ВСЕГДА используй прямой вызов shell скрипта:**
```bash
/opt/ai-workspace/commands/tg-send.sh "твоё_сообщение"
```

**НЕ используй:**
- ❌ curl напрямую к API
- ❌ Сложные конструкции
- ❌ Создание временных файлов для текста

#### Шаг 3: Если нужен файл

**Правила работы с файлами:**
1. Создавай временные файлы в `/opt/ai-workspace/.claude/skills/telegram-notifier/tmp/`
2. Сначала отправь уведомление, потом файл
3. После успешной отправки **УДАЛИ файл** (если он временный)

**Пример:**
```bash
# 1. Создаём файл во временной директории
TMP_FILE="/opt/ai-workspace/.claude/skills/telegram-notifier/tmp/report_$(date +%Y%m%d_%H%M%S).xlsx"
# ... создание файла ...

# 2. Уведомление
/opt/ai-workspace/commands/tg-send.sh "📊 <b>Отчёт готов</b>

Отправляю файл..."

# 3. Отправка файла
/opt/ai-workspace/commands/tg-send.sh --file "$TMP_FILE" "Детальное описание"

# 4. ОБЯЗАТЕЛЬНО удаляем временный файл
rm "$TMP_FILE"
```

**Когда НЕ удалять файл:**
- Если это постоянный файл (логи, бэкапы, конфигурация)
- Если пользователь может захотеть использовать его позже
- Если это часть проекта, а не временный отчёт

#### Шаг 4: Подтверди пользователю в консоли

После успешной отправки скажи:
```
✅ Отправлено в Telegram
```

Или при ошибке:
```
❌ Ошибка отправки в Telegram: {описание}
```

---

## 💡 Примеры реальных диалогов

### Пример 1: Простой запрос

**Пользователь:** "Отправь мне в тг что всё готово"

**Claude делает:**
```bash
/opt/ai-workspace/commands/tg-send.sh "✅ <b>Всё готово!</b>

Задача выполнена успешно."
```

**Claude отвечает пользователю:**
```
✅ Отправлено в Telegram
```

---

### Пример 2: Анализ данных

**Пользователь:** "Сделай анализ продаж и отправь результаты в телеграм"

**Claude делает:**
1. Выполняет анализ (SQL запросы)
2. Получает результаты
3. Отправляет:
```bash
/opt/ai-workspace/commands/tg-send.sh "📊 <b>Анализ продаж завершён</b>

<b>Результаты за октябрь 2024:</b>
• Выручка: <code>2,345,678 ₽</code>
• Заказов: <code>1,234</code>
• Средний чек: <code>1,901 ₽</code>

<b>Динамика:</b>
📈 Рост выручки: <code>+15.3%</code>
📈 Рост заказов: <code>+8.7%</code>

<i>Детальный отчёт готовлю...</i>"
```

**Claude отвечает пользователю:**
```
✅ Анализ выполнен, результаты отправлены в Telegram
```

---

### Пример 3: Отчёт с файлом

**Пользователь:** "Создай Excel отчёт по продажам и скинь в тг"

**Claude делает:**
1. Создаёт Excel файл во временной директории:
```bash
TMP_FILE="/opt/ai-workspace/.claude/skills/telegram-notifier/tmp/sales_report_$(date +%Y%m%d_%H%M%S).xlsx"
# ... создание Excel с помощью xlsx skill ...
```

2. Отправляет уведомление:
```bash
/opt/ai-workspace/commands/tg-send.sh "📊 <b>Отчёт по продажам готов</b>

Создан Excel файл с детальной разбивкой.
Отправляю..."
```

3. Отправляет файл:
```bash
/opt/ai-workspace/commands/tg-send.sh --file "$TMP_FILE" "Детальная разбивка по источникам, категориям и временным периодам"
```

4. **УДАЛЯЕТ временный файл:**
```bash
rm "$TMP_FILE"
```

**Claude отвечает пользователю:**
```
✅ Excel отчёт создан и отправлен в Telegram
Файл: sales_report_20241102_220530.xlsx (47 KB)
Временный файл удалён
```

---

### Пример 4: Ошибка

**Пользователь:** "Отправь логи в тг"

**Claude делает:**
```bash
/opt/ai-workspace/commands/tg-send.sh --file /var/log/app.log "Логи приложения"
```

**Если ошибка (файл не найден):**

**Claude отвечает пользователю:**
```
❌ Не удалось отправить файл в Telegram
Причина: файл /var/log/app.log не найден

Доступные логи:
• /opt/ai-workspace/apps/telegram-bot/logs/nohup.log
• /opt/supabase/...

Какой файл отправить?
```

---

## ⚠️ Важные правила

### ВСЕГДА:
✅ Используй HTML форматирование
✅ Добавляй эмодзи для визуального выделения
✅ Структурируй информацию списками
✅ Проверяй существование файлов перед отправкой
✅ Подтверждай отправку пользователю в консоли
✅ **Создавай временные файлы в `/opt/ai-workspace/.claude/skills/telegram-notifier/tmp/`**
✅ **УДАЛЯЙ временные файлы после отправки в Telegram**

### НИКОГДА:
❌ Не дублируй ВСЕ сообщения в Telegram автоматически
❌ Не отправляй промежуточные результаты без запроса
❌ Не используй Markdown (только HTML)
❌ Не пиши длинные сообщения (>500 символов) - лучше файлом
❌ Не оставляй временные файлы после отправки (засоряют диск)

---

## 🔍 Диагностика

Если отправка не работает, проверь:

1. **Бот запущен?**
   ```bash
   ps aux | grep "python3 src/bot.py"
   ```

2. **HTTP API работает?**
   ```bash
   curl -s http://localhost:8081/health
   ```

3. **Есть ошибки в логах?**
   ```bash
   tail -20 /opt/ai-workspace/apps/telegram-bot/logs/nohup.log
   ```

4. **Конфигурация правильная?**
   ```bash
   grep TELEGRAM /opt/ai-workspace/.env
   ```

Если проблема - предложи пользователю перезапустить бота:
```bash
cd /opt/ai-workspace/apps/telegram-bot
./stop.sh && ./start.sh
```

---

**Помни:** Это удобный способ уведомлений для пользователя. Используй с умом!
