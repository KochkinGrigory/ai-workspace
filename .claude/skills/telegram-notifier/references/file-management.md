# File Management - Управление файлами для отправки в Telegram

## Временная директория

**Путь:** `/opt/ai-workspace/.claude/skills/telegram-notifier/tmp/`

Используй эту директорию для всех файлов, которые создаются только для отправки в Telegram:
- Excel отчёты
- PDF документы
- Графики и визуализации
- Временные HTML страницы
- Экспортированные данные

## Правила именования

Используй timestamp для уникальности:
```bash
# Хорошие примеры
report_$(date +%Y%m%d_%H%M%S).xlsx
analysis_$(date +%Y%m%d_%H%M%S).pdf
chart_$(date +%Y%m%d_%H%M%S).png

# Плохие примеры (могут перезаписаться)
report.xlsx
temp.pdf
output.png
```

## Полный цикл работы с файлом

### Шаг 1: Создание файла
```bash
# Определи путь во временной директории
TMP_FILE="/opt/ai-workspace/.claude/skills/telegram-notifier/tmp/report_$(date +%Y%m%d_%H%M%S).xlsx"

# Создай файл (пример с xlsx skill)
python3 script_to_create_excel.py "$TMP_FILE"

# Или другие способы
cat data.csv > "$TMP_FILE"
```

### Шаг 2: Проверка создания
```bash
# Убедись, что файл создан
if [ ! -f "$TMP_FILE" ]; then
    echo "❌ Файл не был создан"
    exit 1
fi

# Опционально: проверь размер
FILE_SIZE=$(du -h "$TMP_FILE" | cut -f1)
echo "Файл создан: $FILE_SIZE"
```

### Шаг 3: Отправка в Telegram
```bash
# Уведомление
/opt/ai-workspace/commands/tg-send.sh "📊 <b>Отчёт готов</b>

Отправляю файл..."

# Отправка файла
/opt/ai-workspace/commands/tg-send.sh --file "$TMP_FILE" "Описание файла"
```

### Шаг 4: ОБЯЗАТЕЛЬНОЕ удаление
```bash
# После успешной отправки удаляем
if [ $? -eq 0 ]; then
    rm "$TMP_FILE"
    echo "✅ Временный файл удалён"
else
    echo "⚠️ Файл не удалён из-за ошибки отправки"
fi
```

## Полный пример (Excel отчёт)

```bash
#!/bin/bash

# 1. Определяем путь
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
TMP_FILE="/opt/ai-workspace/.claude/skills/telegram-notifier/tmp/sales_report_${TIMESTAMP}.xlsx"

# 2. Создаём отчёт
echo "Создаю Excel отчёт..."
# ... код создания Excel ...

# 3. Проверяем
if [ ! -f "$TMP_FILE" ]; then
    echo "❌ Ошибка создания файла"
    exit 1
fi

FILE_SIZE=$(du -h "$TMP_FILE" | cut -f1)
echo "✅ Файл создан: $FILE_SIZE"

# 4. Отправляем уведомление
/opt/ai-workspace/commands/tg-send.sh "📊 <b>Отчёт по продажам готов</b>

Размер: $FILE_SIZE
Отправляю файл..."

# 5. Отправляем файл
/opt/ai-workspace/commands/tg-send.sh --file "$TMP_FILE" "Детальная разбивка по продажам за октябрь 2024"

# 6. Проверяем результат и удаляем
if [ $? -eq 0 ]; then
    rm "$TMP_FILE"
    echo "✅ Файл отправлен и удалён"
else
    echo "❌ Ошибка отправки, файл сохранён: $TMP_FILE"
fi
```

## Когда НЕ удалять файл

### Постоянные файлы (не удаляем):
- Логи: `/var/log/...`
- Бэкапы: `/opt/.../backups/...`
- Конфигурация: `/etc/...`, `*.conf`
- Документация: `README.md`, `*.md`
- Файлы проекта: исходники, ресурсы

### Временные файлы (удаляем):
- Отчёты для просмотра: `report_*.xlsx`
- Экспорты данных: `export_*.csv`
- Визуализации: `chart_*.png`
- Сгенерированные HTML: `page_*.html`

## Автоматическая очистка старых файлов

Добавь в cron для автоматической очистки старых файлов:

```bash
# Удалять файлы старше 7 дней (на случай если Claude забыл)
0 3 * * * find /opt/ai-workspace/.claude/skills/telegram-notifier/tmp -type f -mtime +7 -delete
```

Или создай скрипт очистки:
```bash
#!/bin/bash
# cleanup-tmp.sh

TMP_DIR="/opt/ai-workspace/.claude/skills/telegram-notifier/tmp"
DAYS_OLD=7

echo "🧹 Очистка временных файлов старше $DAYS_OLD дней..."

# Находим и удаляем старые файлы
DELETED=$(find "$TMP_DIR" -type f -mtime +$DAYS_OLD -delete -print | wc -l)

if [ $DELETED -gt 0 ]; then
    echo "✅ Удалено файлов: $DELETED"
else
    echo "✅ Старых файлов не найдено"
fi
```

## Мониторинг использования диска

Проверяй размер временной директории:
```bash
# Общий размер
du -sh /opt/ai-workspace/.claude/skills/telegram-notifier/tmp

# Список файлов с размерами
du -h /opt/ai-workspace/.claude/skills/telegram-notifier/tmp/* | sort -h

# Количество файлов
ls -1 /opt/ai-workspace/.claude/skills/telegram-notifier/tmp | wc -l
```

Если директория стала слишком большой (>100MB), проверь:
1. Claude забывает удалять файлы?
2. Есть очень большие файлы?
3. Нужна автоматическая очистка?

## Типичные ошибки

### ❌ Плохо: создание файлов где попало
```bash
# В /tmp (может быть очищена системой)
/tmp/report.xlsx

# В домашней директории
~/report.xlsx

# В корне проекта (захламляет)
/opt/ai-workspace/report.xlsx
```

### ✅ Хорошо: во временной директории skill
```bash
/opt/ai-workspace/.claude/skills/telegram-notifier/tmp/report_20241102_220530.xlsx
```

---

### ❌ Плохо: забыть удалить
```bash
# Создал файл
create_report.sh > $TMP_FILE

# Отправил
/opt/ai-workspace/commands/tg-send.sh --file "$TMP_FILE" "Отчёт"

# Забыл удалить - файл остался!
```

### ✅ Хорошо: всегда удалять
```bash
# Создал
create_report.sh > $TMP_FILE

# Отправил
/opt/ai-workspace/commands/tg-send.sh --file "$TMP_FILE" "Отчёт"

# ОБЯЗАТЕЛЬНО удалил
rm "$TMP_FILE"
```

---

## Best Practices

1. **Всегда используй timestamp в имени файла**
2. **Проверяй создание файла перед отправкой**
3. **ВСЕГДА удаляй временные файлы после отправки**
4. **Логируй размер файла перед отправкой**
5. **Не отправляй файлы >50MB (Telegram лимит)**
6. **Храни постоянные файлы в правильных местах** (не в tmp)

---

## Чек-лист для Claude

При работе с файлами для отправки в Telegram:

- [ ] Создал файл в `/opt/ai-workspace/.claude/skills/telegram-notifier/tmp/`
- [ ] Использовал timestamp в имени файла
- [ ] Проверил, что файл создан (`-f`)
- [ ] Отправил уведомление пользователю
- [ ] Отправил файл через tg-send.sh
- [ ] **УДАЛИЛ файл после успешной отправки**
- [ ] Подтвердил пользователю в консоли

**Если забыл удалить - накопятся мусорные файлы!**
