---
name: getcourse-manager
description: "Работа с GetCourse - тренинги, группы, пользователи. ИСПОЛЬЗУЙ ЭТОТ SKILL когда пользователь спрашивает про GetCourse, геткурс, тренинги, курсы на платформе. Триггеры - какие тренинги на GetCourse, список тренингов, покажи тренинги, тренинги на геткурсе, курсы на getcourse, покажи все группы, добавь пользователя в GetCourse, назначь в группу, выдай доступ к тренингу, создай заказ, массовое добавление учеников. Возможности - получение списка групп/тренингов, добавление пользователей, управление доступами."
---

# GetCourse Manager

Автоматизация работы с пользователями, группами и тренингами через GetCourse API.

## 🎯 Когда использовать этот skill

**ВСЕГДА используй этот skill когда пользователь:**
- Хочет добавить пользователей в GetCourse
- Назначить пользователей в группы
- Выдать доступ к тренингам
- Создать заказы для пользователей
- Обновить данные существующих пользователей
- Получить информацию о группах

**Триггерные фразы:**
- "добавь пользователя в GetCourse"
- "создай юзера для тренинга"
- "назначь в группу"
- "выдай доступ к обучению"
- "добавь заказ в GetCourse"

---

## 🔑 Конфигурация

### Переменные окружения

Должны быть настроены в `/opt/ai-workspace/.env`:

```bash
# GetCourse API Configuration
GETCOURSE_ACCOUNT_NAME=your-account    # Имя аккаунта (subdomain)
GETCOURSE_API_KEY=your-secret-key      # Секретный ключ API
```

### Получение API ключа

1. Войдите в админку GetCourse
2. Перейдите в "Настройки" → "API"
3. Сгенерируйте секретный ключ
4. Добавьте в `.env` файл

**Важно:** API недоступен на тарифе "Trial". Требуется платный тариф.

---

## 🚀 Основные возможности

### 1. Добавление пользователя

```bash
python3 scripts/add_user.py \
  --email user@example.com \
  --first-name "Иван" \
  --last-name "Петров" \
  --phone "+79001234567" \
  --groups "Тренинг 1" "Тренинг 2"
```

**Минимальные требования:** Email ИЛИ телефон (хотя бы одно поле обязательно).

### 2. Получение списка групп

```bash
python3 scripts/get_groups.py
```

Возвращает JSON со всеми группами аккаунта.

### 3. Назначение в группы (обновление)

```bash
python3 scripts/update_user_groups.py \
  --email user@example.com \
  --groups "Новая группа 1" "Новая группа 2"
```

**Важно:** Заменяет ВСЕ существующие группы на указанные (не добавляет).

### 4. Создание заказа

```bash
python3 scripts/create_deal.py \
  --email user@example.com \
  --offer-code "COURSE2024" \
  --cost 5990 \
  --is-paid
```

---

## 📚 Документация API

### Базовый формат запроса

**Структура:**
```
POST https://{account_name}.getcourse.ru/pl/api/{endpoint}

Параметры:
- action: add/update/get
- key: {secret_key}
- params: {base64_encoded_json}
```

**Пример params (до кодирования):**
```json
{
  "user": {
    "email": "user@example.com",
    "phone": "+79001234567",
    "first_name": "Иван",
    "last_name": "Петров",
    "city": "Москва",
    "country": "Россия",
    "group_name": ["Группа1", "Группа2"]
  },
  "system": {
    "refresh_if_exists": 1
  }
}
```

### Основные эндпоинты

| Эндпоинт | Назначение |
|----------|------------|
| `/pl/api/users` | Добавление/обновление пользователей |
| `/pl/api/deals` | Создание заказов |
| `/pl/api/account/groups` | Получение списка групп |
| `/pl/api/messages` | Отправка сообщений |

### Формат ответа

```json
{
  "success": "true",
  "action": "add",
  "result": {
    "success": "true",
    "user_id": "12345",
    "user_status": "active",
    "error": "false"
  }
}
```

---

## 🎓 Как работает доступ к тренингам

### Связь: Группы → Тренинги

В GetCourse доступ к тренингам выдается через **группы**:

1. **Создайте группу** в админке GetCourse
2. **Привяжите группу к тренингу** в настройках тренинга
3. **Добавьте пользователя в группу** через API
4. ✅ Пользователь автоматически получит доступ к тренингу

**Пример:**
```
Группа "Курс SMM 2024" → Тренинг "Продвижение в соцсетях"
                       ↓
Добавили Ивана в группу "Курс SMM 2024"
                       ↓
✅ Иван получил доступ к "Продвижение в соцсетях"
```

### Способы назначения в группы

**1. При создании пользователя:**
```json
{
  "user": {
    "email": "user@example.com",
    "group_name": ["Группа1", "Группа2"]
  }
}
```

**2. При создании заказа:**
Если продукт настроен на добавление в группу при покупке, пользователь автоматически попадет в группу при создании оплаченного заказа.

**3. Обновление существующего пользователя:**
```json
{
  "user_id": "12345",
  "group_name": ["Новая группа"]
}
```

---

## 🛠️ Технические детали

### Обязательные поля

**Для добавления пользователя:**
- `email` (если нет телефона)
- `phone` (если нет email)

**Для создания заказа:**
- `email` пользователя
- `offer_code` ИЛИ `offer_id`
- `deal_cost` (если не указан в оффере)

### Дополнительные поля пользователя

Можно передавать кастомные поля через `addfields`:
```json
{
  "user": {
    "email": "user@example.com",
    "addfields": {
      "Telegram": "@username",
      "Источник": "Реклама VK",
      "Дата регистрации": "01.01.2024"
    }
  }
}
```

### Флаг refresh_if_exists

```json
{
  "system": {
    "refresh_if_exists": 1  // 1 = обновить, 0 = только создать
  }
}
```

- `1` - если пользователь существует, обновить его данные
- `0` - если пользователь существует, вернуть ошибку

---

## 📋 Типичные сценарии использования

### Сценарий 1: Добавить нового студента на тренинг

```bash
# 1. Узнать название группы
python3 scripts/get_groups.py | jq -r '.result.groups[] | select(.title | contains("SMM")) | .title'

# 2. Добавить пользователя в эту группу
python3 scripts/add_user.py \
  --email ivan@example.com \
  --first-name "Иван" \
  --last-name "Петров" \
  --phone "+79001234567" \
  --groups "Курс SMM 2024"

# ✅ Иван получил доступ к тренингу
```

### Сценарий 2: Массовое добавление из списка

```bash
# Если пользователь дал список email:
# ivan@example.com, Иван, Петров
# maria@example.com, Мария, Сидорова

cat users.csv | while IFS=, read email first_name last_name; do
  python3 scripts/add_user.py \
    --email "$email" \
    --first-name "$first_name" \
    --last-name "$last_name" \
    --groups "Тренинг Основной"
  sleep 1  # Пауза между запросами
done
```

### Сценарий 3: Создать заказ и выдать доступ

```bash
# 1. Создать оплаченный заказ (если оффер настроен на добавление в группу)
python3 scripts/create_deal.py \
  --email user@example.com \
  --offer-code "TRAINING2024" \
  --cost 9990 \
  --is-paid

# 2. Пользователь автоматически попадает в группу и получает доступ
```

---

## ⚠️ Важные ограничения

### Лимиты API

- **Import API:** Лимиты на создание объектов в месяц (зависят от тарифа)
- **Export API:** 100 запросов за 2 часа
- **Trial тариф:** API недоступен

### Безопасность

- Используй только HTTPS
- Храни API ключ в `.env` (не коммить!)
- Не передавай ключ в URL (только POST body)

### Работа с группами

- При обновлении групп передается **полный список** групп
- Группы не в списке будут **удалены** у пользователя
- Названия групп регистрозависимы

---

## 🔍 Отладка и логирование

### Проверка конфигурации

```bash
# Проверить наличие переменных окружения
grep -E "GETCOURSE_" /opt/ai-workspace/.env

# Тест подключения (получить список групп)
python3 scripts/get_groups.py
```

### Обработка ошибок

Скрипты возвращают JSON с полями:
- `success`: true/false
- `result.error`: true/false
- `result.error_message`: описание ошибки

**Типичные ошибки:**
- `"Invalid key"` - неверный API ключ
- `"User already exists"` - пользователь существует (нужен `refresh_if_exists: 1`)
- `"Email or phone required"` - не указан email или телефон
- `"Group not found"` - группа с таким названием не существует

---

## 📖 Дополнительные ресурсы

**Официальная документация:**
- [GetCourse API](https://getcourse.ru/help/api) - основная документация
- [PHP SDK](https://github.com/getcourse-ru/GetCourse-PHP-SDK) - примеры кода
- [Импорт пользователей](https://getcourse.ru/blog/276058) - дополнительные способы

**Справочные материалы:**
- [api-reference.md](references/api-reference.md) - подробная документация API
- [groups-and-training.md](references/groups-and-training.md) - как работают группы и доступы

---

## 🎯 Best Practices

### 1. Всегда используй refresh_if_exists

```json
{
  "system": {
    "refresh_if_exists": 1
  }
}
```

Это позволяет избежать ошибок при повторном добавлении.

### 2. Проверяй названия групп

```bash
# Сначала получи список групп
python3 scripts/get_groups.py

# Потом используй ТОЧНОЕ название (с учетом регистра)
python3 scripts/add_user.py --groups "Название Группы Точно Как В Системе"
```

### 3. Логируй все операции

```bash
# Сохраняй результаты в лог
python3 scripts/add_user.py ... >> /opt/ai-workspace/logs/getcourse_$(date +%Y%m%d).log
```

### 4. Пауза между массовыми запросами

```bash
# При массовом добавлении делай паузу
sleep 1  # 1 секунда между запросами
```

---

## 🔄 Интеграция с другими skills

### С xlsx skill

```bash
# Пользователь загружает Excel с учениками
# 1. xlsx skill → прочитать данные
# 2. getcourse-manager → добавить каждого ученика
# 3. Вернуть отчет о добавленных
```

### С telegram-notifier

```bash
# После добавления пользователя
# Отправить уведомление в Telegram
/tg "✅ Добавлен новый студент: Иван Петров (ivan@example.com)"
```

---

## 📊 Мониторинг и отчетность

### Создание отчета о добавленных пользователях

После массового добавления создавай отчет:

```
✅ Добавлено пользователей: 15
❌ Ошибок: 2

Успешно:
• ivan@example.com → Группа "Тренинг 1"
• maria@example.com → Группа "Тренинг 1"
...

Ошибки:
• duplicate@example.com - User already exists
• invalid@email - Invalid email format
```

---

## 🎓 Примеры использования из разговора

Когда пользователь говорит:
- "Добавь Ивана в тренинг SMM" → `add_user.py` с группой "SMM"
- "Выдай доступ Марии к курсу" → `add_user.py` с соответствующей группой
- "Создай заказ для клиента" → `create_deal.py`
- "Покажи все группы" → `get_groups.py`

---

## 📝 TODO для улучшения skill

- [ ] Добавить скрипт для массового импорта из CSV
- [ ] Реализовать экспорт пользователей
- [ ] Добавить работу с дополнительными полями
- [ ] Создать интерактивный режим добавления
- [ ] Интеграция с процессами GetCourse
- [ ] Отправка email через GetCourse API

---

**Версия:** 1.0
**Дата создания:** 2026-01-06
**Автор:** Claude Code with Telegram Agent
