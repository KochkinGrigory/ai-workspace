# Группы и доступ к тренингам в GetCourse

> Как работает система групп и выдача доступов к обучению

## 🎯 Основная концепция

В GetCourse **доступ к тренингам предоставляется через группы**:

```
Пользователь → Группа → Тренинг
```

**Не напрямую**, а через промежуточное звено - группу.

---

## 📚 Как это работает

### Шаг 1: Создание группы

В админке GetCourse:
1. **Пользователи** → **Группы**
2. **Создать группу**
3. Указать название, описание

**Пример групп:**
- "Курс SMM 2024"
- "VIP студенты"
- "Поток №5 - Март 2024"
- "Бесплатный доступ"

### Шаг 2: Привязка группы к тренингу

В настройках тренинга:
1. **Тренинги** → выбрать тренинг
2. **Настройки** → **Доступ**
3. **Добавить группу** для доступа

**Результат:** Все участники группы получают доступ к тренингу.

### Шаг 3: Добавление пользователей в группу

**Способы:**

#### 1. Через API (наш случай)
```python
python3 add_user.py \
  --email user@example.com \
  --groups "Курс SMM 2024"
```

#### 2. При покупке продукта
В настройках продукта указать: "При покупке добавить в группу X"

#### 3. Вручную из админки
Карточка пользователя → Группы → Добавить

#### 4. Через процессы (автоматизация)
Настроить процесс: "При событии X → добавить в группу Y"

#### 5. Через форму регистрации
Обработчик формы → Добавить в группу

---

## 🔄 Схема работы

### Типичный сценарий 1: Бесплатный тренинг

```
1. Пользователь регистрируется на сайте
   ↓
2. API добавляет его в группу "Бесплатный курс"
   ↓
3. Группа дает доступ к тренингу "Основы SMM"
   ↓
4. ✅ Пользователь видит тренинг в личном кабинете
```

### Типичный сценарий 2: Платный тренинг

```
1. Пользователь оплачивает продукт "Курс PRO"
   ↓
2. Создается заказ через API (deal_is_paid=1)
   ↓
3. Продукт настроен на добавление в группу "Студенты PRO"
   ↓
4. Группа дает доступ к тренингу "PRO курс"
   ↓
5. ✅ Пользователь получает доступ автоматически
```

### Типичный сценарий 3: Поток обучения

```
1. Набирается новый поток (например, март 2024)
   ↓
2. Создается группа "Поток №5 - Март 2024"
   ↓
3. Группа привязывается к тренингу "Основной курс"
   ↓
4. Через API массово добавляются все студенты потока
   ↓
5. ✅ Весь поток получает доступ одновременно
```

---

## 🎓 Типы доступов

### 1. Полный доступ
Пользователь видит все уроки тренинга сразу.

**Настройка:** Группа → Тренинг → Тип доступа: "Полный"

### 2. Постепенный доступ (дриппинг)
Уроки открываются по расписанию.

**Настройка:** Группа → Тренинг → Тип доступа: "Постепенный"
- Открывать по 1 уроку в день
- Открывать по датам
- Открывать после прохождения предыдущего

### 3. Временный доступ
Доступ на определенный период.

**Настройка:** Группа → Период доступа: 30 дней

### 4. Условный доступ
Доступ зависит от выполнения условий.

**Настройка:** Через процессы и правила

---

## 📊 Управление группами через API

### Получение списка групп

```bash
python3 get_groups.py
```

**Ответ:**
```json
{
  "success": "true",
  "result": {
    "groups": [
      {
        "id": 1,
        "title": "Курс SMM 2024",
        "users_count": 150
      },
      {
        "id": 2,
        "title": "VIP студенты",
        "users_count": 25
      }
    ]
  }
}
```

### Добавление пользователя в группы

**При создании:**
```python
python3 add_user.py \
  --email user@example.com \
  --groups "Группа 1" "Группа 2"
```

**Обновление существующего:**
```python
python3 update_user_groups.py \
  --email user@example.com \
  --groups "Новая группа"
```

⚠️ **Важно:** При обновлении передается ПОЛНЫЙ список групп. Группы не в списке будут удалены!

---

## 🔐 Сценарии использования групп

### 1. Разделение по тарифам

```
Группа "Базовый тариф"  → Тренинг "Основы" (10 уроков)
Группа "Продвинутый"    → Тренинг "Основы" + "Продвинутый" (30 уроков)
Группа "VIP"            → Все тренинги + личные консультации
```

### 2. Разделение по потокам

```
Группа "Поток №1 - Январь 2024"
Группа "Поток №2 - Февраль 2024"
Группа "Поток №3 - Март 2024"
```

Каждая группа получает доступ к одному тренингу, но в разное время.

### 3. Разделение по статусам

```
Группа "Новички"          → Доступ к вводным урокам
Группа "Активные"         → Доступ к основному курсу
Группа "Выпускники"       → Доступ ко всем материалам навсегда
Группа "Отказались"       → Доступ закрыт
```

### 4. Сегментация для маркетинга

```
Группа "Пришли из VK"     → Рассылка специальных предложений
Группа "Пришли из Google" → Другая воронка
Группа "Рефералы"         → Бонусные материалы
```

---

## 🎯 Автоматизация доступов

### Через продукты

**Настройка продукта:**
1. Создать продукт "Курс SMM"
2. В настройках: **"При покупке добавить в группу"** → выбрать группу
3. Группа уже привязана к тренингу

**Результат:**
```
Оплата продукта → Автоматически в группу → Автоматически доступ к тренингу
```

### Через процессы

**Пример процесса:**
```
Триггер: "Заполнена форма регистрации"
   ↓
Действие 1: "Создать пользователя"
   ↓
Действие 2: "Добавить в группу 'Бесплатный курс'"
   ↓
Действие 3: "Отправить письмо с доступом"
```

### Через API

**Пример скрипта:**
```python
# 1. Добавить пользователя
result = add_user(
    email="user@example.com",
    first_name="Иван",
    groups=["Курс SMM 2024"]
)

# 2. Пользователь автоматически получает доступ к тренингу
# (если группа привязана к тренингу)

# 3. Отправить уведомление
send_notification("Доступ к курсу открыт!")
```

---

## 📋 Чек-лист настройки доступа

### Перед добавлением пользователей через API

- [ ] Создана группа в админке GetCourse
- [ ] Группа привязана к нужному тренингу
- [ ] Настроен тип доступа (полный/постепенный/временный)
- [ ] Проверено название группы (точное соответствие!)
- [ ] Тренинг опубликован (не в черновиках)

### После добавления пользователя

- [ ] Проверить через API, что пользователь в группе
- [ ] Зайти в админку → убедиться что пользователь в списке группы
- [ ] Зайти в личный кабинет студента → проверить доступ к тренингу
- [ ] Проверить что уроки видны согласно настройкам доступа

---

## 🔍 Отладка проблем с доступом

### Проблема: Пользователь добавлен, но не видит тренинг

**Чек-лист:**

1. **Проверить группу пользователя:**
   ```bash
   # В админке: Пользователи → Найти → Группы
   ```

2. **Проверить привязку группы к тренингу:**
   ```bash
   # Тренинги → Выбрать → Настройки → Доступ → Группы
   ```

3. **Проверить статус тренинга:**
   - Опубликован ли?
   - Есть ли уроки?
   - Настроен ли доступ?

4. **Проверить написание названия группы:**
   ```bash
   # Регистрозависимо!
   "Курс SMM" ≠ "курс smm" ≠ "Курс SMM " (пробел в конце)
   ```

5. **Проверить тип доступа:**
   - Если "Постепенный" - возможно уроки еще не открылись
   - Если "Временный" - возможно срок истек

### Проблема: Группа не найдена при добавлении

**Решение:**
```bash
# 1. Получить точный список групп
python3 get_groups.py

# 2. Найти нужную группу
python3 get_groups.py | jq -r '.result.groups[] | select(.title | contains("SMM"))'

# 3. Скопировать ТОЧНОЕ название
# 4. Использовать в add_user.py
```

---

## 💡 Best Practices

### 1. Используйте понятные названия групп

```
✅ Хорошо:
- "Курс SMM 2024 - Поток №5"
- "VIP студенты - Все курсы"
- "Бесплатный доступ - Основы"

❌ Плохо:
- "Группа1"
- "Test"
- "qwerty"
```

### 2. Разделяйте группы по назначению

```
Группы для доступа к тренингам:
- "Доступ: Курс SMM"
- "Доступ: Курс Продажи"

Группы для сегментации:
- "Сегмент: VK трафик"
- "Сегмент: Рефералы"

Группы для рассылок:
- "Рассылка: Еженедельная"
- "Рассылка: Спецпредложения"
```

### 3. Документируйте привязки

```markdown
# Наши группы и тренинги

## Курс SMM 2024
- Группа: "Курс SMM 2024"
- Тренинг: "Продвижение в соцсетях"
- Доступ: Полный
- Период: Бессрочно

## VIP программа
- Группа: "VIP студенты"
- Тренинги: Все курсы + бонусы
- Доступ: Полный + личные консультации
- Период: 1 год
```

### 4. Автоматизируйте процессы

Вместо ручного добавления:
```python
# Скрипт для массового добавления нового потока
students = read_csv("students_march_2024.csv")

for student in students:
    add_user(
        email=student['email'],
        first_name=student['first_name'],
        groups=["Поток №5 - Март 2024"]
    )
```

---

## 📊 Аналитика по группам

### Получение статистики

```bash
# Количество пользователей в каждой группе
python3 get_groups.py | jq -r '.result.groups[] | "\(.title): \(.users_count)"'
```

**Вывод:**
```
Курс SMM 2024: 150
VIP студенты: 25
Бесплатный доступ: 1200
```

### Экспорт пользователей группы

```bash
# Через Export API
curl "https://account.getcourse.ru/pl/api/account/groups/123/users?key=$API_KEY"
```

---

## 🎓 Примеры готовых решений

### Решение 1: Автоматическая выдача бесплатного курса

```python
def give_free_course_access(email, name):
    """
    Выдать доступ к бесплатному курсу
    """
    result = add_user(
        email=email,
        first_name=name,
        groups=["Бесплатный курс - Основы SMM"],
        utm_source="landing",
        utm_medium="free_course"
    )

    if result['success']:
        # Отправить email с инструкцией
        send_welcome_email(email)

    return result
```

### Решение 2: Продажа со скидкой для группы

```python
def create_vip_deal(email, discount_code):
    """
    Создать заказ с VIP скидкой
    """
    # 1. Создать заказ
    deal = create_deal(
        email=email,
        offer_code="VIP_COURSE",
        cost=4990,  # Со скидкой вместо 9990
        is_paid=True
    )

    # 2. Добавить в VIP группу
    add_user(
        email=email,
        groups=["VIP студенты"]
    )

    return deal
```

### Решение 3: Миграция пользователей между потоками

```python
def migrate_to_new_stream(emails, new_stream_group):
    """
    Перевести пользователей в новый поток
    """
    for email in emails:
        # Обновить группу (старая удалится автоматически)
        update_user_groups(
            email=email,
            groups=[new_stream_group]
        )

    print(f"Migrated {len(emails)} users to {new_stream_group}")
```

---

## 📖 Дополнительные материалы

**В GetCourse:**
- Справка → Группы пользователей
- Справка → Настройка доступа к тренингу
- Справка → Процессы и автоматизация

**В нашем проекте:**
- `SKILL.md` - основная документация skill
- `api-reference.md` - техническая документация API
- `examples.md` - примеры кода
