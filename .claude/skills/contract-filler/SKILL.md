---
name: contract-filler
description: |
  Автоматическое заполнение договоров данными компании с генерацией Word и PDF. ИСПОЛЬЗУЙ ЭТОТ SKILL КОГДА: пользователь просит (1) заполнить договор, (2) подставить данные компании в документ, (3) сгенерировать договор из шаблона, (4) создать PDF из Word договора, (5) автоматизировать заполнение реквизитов. Skill работает как человек: читает договор полностью, понимает контекст и структуру, определяет какие поля нужно заполнить, точечно редактирует каждый параграф. Поддерживает любые типы договоров (благотворительность, поставка, услуги, подряд и др.). Автоматически определяет роль компании (заказчик/исполнитель/подрядчик). Интерактивно спрашивает переменные данные (суммы, даты, описания). Генерирует оба формата одновременно: редактируемый .docx и финальный .pdf с именем YYYY-MM-DD_Договор_[компания].
allowed-tools: Read, Write, Edit, Bash(cmd:*), Bash(python:*), Bash(python3:*), AskUserQuestion
version: 1.0.0
author: Contract Automation Team
license: MIT
---

# Contract Filler - Автоматическое заполнение договоров

Автоматизация заполнения договоров данными компании с AI-анализом структуры документа.

---

## 🎯 Когда использовать этот skill

**ВСЕГДА активируй этот skill когда пользователь:**
- "Заполни договор данными компании"
- "Подставь реквизиты в договор"
- "Сгенерируй PDF из договора"
- "Автоматизируй заполнение этого договора"
- "Заполни от имени [название компании]"

**Триггерные слова:** заполнить договор, подставить данные, реквизиты компании, сгенерировать PDF, автозаполнение

---

## 🔥 Ключевая особенность

**Skill работает КАК ЧЕЛОВЕК, а НЕ как "найти-заменить":**

❌ **НЕ так (плохо):**
```python
# Поиск паттернов во всем документе
if "ИНН:" in text:
    text = text.replace("ИНН:", f"ИНН: {company.inn}")
# Проблема: может заменить ИНН организатора вместо заказчика!
```

✅ **ТАК (правильно):**
```python
# 1. Читаем весь договор
full_text = read_all_paragraphs(doc)

# 2. AI анализирует структуру
"""
Параграф 2: Заголовок с номером договора
Параграф 4: Дата
Параграф 5: Название ПРЕДПРИЯТИЯ (заказчик)
Параграф 24: ИНН ПРЕДПРИЯТИЯ (это правая колонка, заказчик)
Параграф 25: ИНН ОРГАНИЗАТОРА (это левая колонка, не трогаем)
"""

# 3. Точечно редактируем
paragraphs[23].text = paragraphs[23].text.replace(
    '__________________________________________',
    company['name_short']
)  # Редактируем ТОЛЬКО нужный параграф
```

**Результат:** Точность 100%, никаких случайных замен!

---

## 📋 Основной Workflow

### Шаг 1: Получение договора

```python
User: "Заполни договор /path/to/contract.docx от analiticalo"
```

**Skill делает:**
1. Читает файл договора
2. Извлекает все параграфы
3. Читает полный текст для понимания контекста

---

### Шаг 2: AI-анализ структуры договора

**Skill анализирует документ как человек:**

```python
# Читаем весь договор
for i, para in enumerate(doc.paragraphs):
    print(f"[{i}] {para.text}")

# AI понимает:
# - Где заголовок с номером договора
# - Где дата
# - Где стороны (кто заказчик, кто исполнитель)
# - Где реквизиты (левая/правая колонка)
# - Где переменные поля (суммы, описания)
# - Где подписи
```

**Ключевые паттерны для анализа:**
- `№ __________` → номер договора
- `«___» __________202Хг.` → дата
- `именуемый/именуемое в дальнейшем "ПРЕДПРИЯТИЕ"` → заказчик
- `именуемый/именуемое в дальнейшем "ОРГАНИЗАТОР"` → исполнитель
- `ИНН:\t...` (с табуляцией справа) → реквизиты второй стороны
- `в размере: ___________` → сумма
- `на общую сумму ___________` → итоговая сумма
- Подчеркивания `__________` → поля для заполнения

---

### Шаг 3: Определение роли компании

**Skill автоматически определяет:**

```python
# Анализ текста договора
if 'именуемое в дальнейшем «ПРЕДПРИЯТИЕ»' in contract_text:
    # Наша компания = ПРЕДПРИЯТИЕ
    our_role = "заказчик"
    our_section = "правая колонка реквизитов"

elif 'именуемый в дальнейшем «ОРГАНИЗАТОР»' in contract_text:
    # Наша компания = ОРГАНИЗАТОР
    our_role = "исполнитель"
    our_section = "левая колонка реквизитов"
```

**Если не понятно:**
```
Skill: "В договоре упоминаются стороны:
  - ПРЕДПРИЯТИЕ
  - ОРГАНИЗАТОР

  От имени кого заполняем договор? (заказчик/исполнитель)"
```

---

### Шаг 4: Загрузка данных компании

```python
import json

# Читаем базу компаний
with open('.claude/skills/contract-filler/data/companies.json') as f:
    companies = json.load(f)

# Если пользователь указал компанию
if user_specified_company in companies:
    company_data = companies[user_specified_company]
else:
    # Спрашиваем
    Skill: "Доступные компании:
      - analiticalo
      - example_company

      От имени какой компании заполнить?"
```

---

### Шаг 5: Сбор переменных данных

**Skill интерактивно спрашивает:**

```
Skill: "Номер договора? (например: 15-БП/2025)"
User: "25-БП/2025"

Skill: "Дата договора? (например: 20 января 2025г.)"
User: "20 января 2025г."

Skill: "Сумма финансовой помощи? (например: 150 000 (сто пятьдесят тысяч) рублей)"
User: "150 000 (сто пятьдесят тысяч) рублей"

Skill: "Количество призов? (например: 20)"
User: "20"

Skill: "Стоимость призов? (например: 30 000)"
User: "30 000"

Skill: "Описание услуг? (например: Предоставление помещения)"
User: "Предоставление техники и оборудования"

Skill: "Стоимость услуг? (например: 50 000)"
User: "50 000"
```

**Примечание:** Список вопросов зависит от структуры договора. Skill анализирует какие поля есть и спрашивает только нужное.

---

### Шаг 6: Точечное заполнение

**Skill редактирует каждый параграф по индексу:**

```python
# Пример: Заполнение номера договора
paragraphs[1].text = f"ДОГОВОР ОБ ОКАЗАНИИ БЛАГОТВОРИТЕЛЬНОЙ ПОМОЩИ № {contract_number}"

# Пример: Заполнение даты
paragraphs[3].text = f"Город КИРОВ \t\t\t\t\t\t\t «{contract_date}»"

# Пример: Название компании в шапке
para5_text = paragraphs[4].text
parts = para5_text.split('с одной стороны и ')
if len(parts) == 2:
    before = parts[0] + 'с одной стороны и '
    after = (f'{company["name_full"]}, именуемое в дальнейшем «ПРЕДПРИЯТИЕ», '
             f'в лице {company["director_title"]} {company["director_name"]},')
    paragraphs[4].text = before + after

# Пример: Реквизиты (с сохранением табуляции)
para25 = paragraphs[24].text
parts = para25.split('\t')
parts[-1] = f"ИНН:\t{company['inn']}"
paragraphs[24].text = '\t'.join(parts)
```

**Ключевые принципы:**
1. **Работаем по индексам параграфов** - точно знаем что редактируем
2. **Сохраняем табуляцию** - структура реквизитов не ломается
3. **Заменяем ТОЛЬКО нужные части** - не трогаем весь текст
4. **Понимаем контекст** - знаем что параграф 24 = ИНН заказчика, а не исполнителя
5. **ОБЯЗАТЕЛЬНО удаляем пустые подчеркивания** - договор должен выглядеть аккуратно

**Удаление пустых подчеркиваний:**

```python
# После заполнения всех полей, убираем оставшиеся подчеркивания
for para in doc.paragraphs:
    # Удаляем длинные подчеркивания (более 5 подряд)
    if '_____' in para.text:
        # Если в параграфе есть значимый текст, убираем только лишние подчеркивания
        # Оставляем подчеркивания для подписей "___________"
        if 'Президент' not in para.text and 'Директор' not in para.text:
            # Убираем подчеркивания в начале или середине строки
            para.text = para.text.replace('___________________________________________________________________________________', '')
            para.text = para.text.replace('_______________________________________', '')
            para.text = para.text.replace('_______________', '').strip()
```

**Важно:** Подчеркивания для подписей ("Директор ___________") оставляем - они нужны для реальной подписи.

---

### Шаг 7: Генерация файлов

**Создаём оба формата одновременно:**

```python
import datetime
import subprocess

# Определяем имя контрагента из договора
counterparty = extract_counterparty_name(doc)  # например "ТСК Надежда"

# Формируем имя файла
today = datetime.date.today().strftime('%Y-%m-%d')
company_name = company_key  # например "фонд"
base_name = f"{today}_Договор_{company_name}_{counterparty}"

# Путь сохранения: отдельная папка для договоров
output_dir = "/opt/ai-workspace/.claude/skills/telegram-notifier/uploads/contracts/"
docx_path = f"{output_dir}{base_name}.docx"
pdf_path = f"{output_dir}{base_name}.pdf"

# Сохраняем Word
doc.save(docx_path)

# Конвертируем в PDF через unoconv
subprocess.run(['unoconv', '-f', 'pdf', docx_path], cwd=output_dir)

# Возвращаем пользователю
print(f"""
✅ ДОГОВОР ЗАПОЛНЕН И ГОТОВ!

📁 Созданные файлы:
  • {base_name}.docx (редактируемый Word)
  • {base_name}.pdf (финальный PDF)

📍 Расположение: {output_dir}
""")
```

**Правила именования:**
- Формат: `YYYY-MM-DD_Договор_[наша_компания]_[контрагент].{docx,pdf}`
- Пример: `2026-01-05_Договор_фонд_ТСК-Надежда.docx`
- Обе компании в имени для быстрого поиска

---

## 📂 Структура данных компаний

**Файл:** `.claude/skills/contract-filler/data/companies.json`

```json
{
  "companies": {
    "company_key": {
      "name_full": "Полное название с ОПФ",
      "name_short": "Короткое название",
      "inn": "ИНН",
      "kpp": "КПП",
      "ogrn": "ОГРН",
      "director_title": "Должность руководителя",
      "director_name": "ФИО полностью",
      "director_name_genitive": "ФИО в родительном падеже",
      "director_short": "И.О. Фамилия",
      "basis": "Основание полномочий (Устава/Доверенности)",
      "account": "Расчетный счет",
      "bank": "Название банка",
      "bik": "БИК",
      "korr_account": "Корреспондентский счет",
      "address": "Юридический адрес",
      "phone": "Телефон",
      "email": "Email"
    }
  }
}
```

**Все поля обязательны** для корректного заполнения договоров.

---

## 🔧 Технические детали

### Зависимости

- **python3-docx** - работа с Word документами
- **unoconv** - конвертация в PDF через LibreOffice
- **LibreOffice** - для пересчета формул и конвертации

```bash
apt-get install python3-docx unoconv libreoffice-writer
```

### Алгоритм работы

```
1. Читаем договор → doc.paragraphs[]
2. AI анализирует структуру → понимает контекст
3. Определяем роль компании → заказчик/исполнитель
4. Загружаем данные → companies.json
5. Спрашиваем переменные → интерактивно
6. Точечно редактируем → paragraphs[i].text
7. Сохраняем .docx → doc.save()
8. Конвертируем .pdf → unoconv -f pdf
9. Возвращаем оба файла → пользователю
```

---

## 💡 Best Practices

### ✅ DO (Делай так):

1. **Всегда читай весь договор сначала**
   ```python
   # Сначала анализ
   full_text = '\n'.join([p.text for p in doc.paragraphs])
   # Потом редактирование
   ```

2. **Работай по индексам параграфов**
   ```python
   paragraphs[24].text = ...  # Точно знаешь что редактируешь
   ```

3. **Сохраняй табуляцию в реквизитах**
   ```python
   parts = para.text.split('\t')
   parts[-1] = f"ИНН:\t{inn}"
   para.text = '\t'.join(parts)
   ```

4. **Спрашивай, если не уверен**
   ```python
   if unclear_role:
       AskUserQuestion("От имени кого заполняем?")
   ```

5. **Показывай превью данных**
   ```python
   print("Заполню следующие данные:")
   for key, value in data.items():
       print(f"  • {key}: {value}")
   ```

6. **Удаляй пустые подчеркивания**
   ```python
   # После заполнения убираем лишние подчеркивания
   for para in doc.paragraphs:
       if '_____' in para.text and 'Директор' not in para.text:
           para.text = para.text.replace('_______________', '').strip()
   ```

7. **Включай обе компании в имя файла**
   ```python
   # Формат: YYYY-MM-DD_Договор_[наша]_[контрагент]
   base_name = f"{today}_Договор_{company_key}_{counterparty}"
   ```

8. **Сохраняй в отдельную папку для договоров**
   ```python
   output_dir = "/opt/ai-workspace/.claude/skills/telegram-notifier/uploads/contracts/"
   doc.save(f"{output_dir}{base_name}.docx")
   ```

### ❌ DON'T (Не делай так):

1. **НЕ ищи паттерны глобально**
   ```python
   # ❌ Плохо
   for para in doc.paragraphs:
       if "ИНН:" in para.text:  # Может задеть чужой ИНН!
   ```

2. **НЕ заменяй весь параграф**
   ```python
   # ❌ Плохо
   para.text = "Новый текст"  # Потерялось форматирование

   # ✅ Хорошо
   para.text = para.text.replace("старое", "новое")
   ```

3. **НЕ забывай про родительный падеж**
   ```python
   # ❌ Плохо: "в лице Иванов Иван Иванович"
   # ✅ Хорошо: "в лице Иванова Ивана Ивановича"
   ```

4. **НЕ игнорируй табуляцию**
   ```python
   # ❌ Плохо: "ИНН:   7707123456" (пробелы)
   # ✅ Хорошо: "ИНН:\t7707123456" (табуляция)
   ```

5. **НЕ оставляй пустые подчеркивания**
   ```python
   # ❌ Плохо: в договоре остались "_______________"
   # ✅ Хорошо: убрать все незаполненные подчеркивания
   ```

6. **НЕ сохраняй в корень проекта или общие папки**
   ```python
   # ❌ Плохо: /opt/ai-workspace/contract.docx
   # ❌ Плохо: uploads/files/ (смешивается с другими файлами)
   # ✅ Хорошо: uploads/contracts/ (отдельная папка только для договоров)
   ```

7. **НЕ используй только одну компанию в имени**
   ```python
   # ❌ Плохо: 2026-01-05_Договор_фонд.docx
   # ✅ Хорошо: 2026-01-05_Договор_фонд_ТСК-Надежда.docx
   ```

---

## 📖 Примеры использования

### Пример 1: Базовое использование

```
User: "Заполни договор /opt/ai-workspace/contract.docx от analiticalo"

Skill:
  [Читает договор]
  [Анализирует структуру]

  "Обнаружил договор о благотворительной помощи.
   Определена роль: ПРЕДПРИЯТИЕ (заказчик)

   Номер договора?"

User: "25-БП/2025"

Skill: "Дата договора?"
User: "20 января 2025г."

Skill: "Сумма финансовой помощи?"
User: "200 000 (двести тысяч) рублей"

[... остальные вопросы ...]

Skill:
  ✅ ДОГОВОР ЗАПОЛНЕН!

  📁 Файлы:
    • 2026-01-05_Договор_analiticalo.docx
    • 2026-01-05_Договор_analiticalo.pdf
```

### Пример 2: С уточнением роли

```
User: "Заполни договор contract.docx"

Skill:
  [Анализирует]

  "Доступные компании:
    - analiticalo
    - technoservice

   От имени какой компании заполнить?"

User: "analiticalo"

Skill:
  "В договоре две стороны:
    - ЗАКАЗЧИК
    - ИСПОЛНИТЕЛЬ

   Мы выступаем как? (заказчик/исполнитель)"

User: "исполнитель"

Skill: "Понятно, заполняю данные в раздел ИСПОЛНИТЕЛЬ..."
```

### Пример 3: Пропуск необязательных полей

```
Skill: "Количество призов? (Enter = пропустить)"
User: [Enter]

Skill: "Хорошо, пропускаю это поле."
```

---

## 🐛 Troubleshooting

### Проблема 1: Реквизиты в неправильном месте

**Причина:** Неправильно определена роль компании

**Решение:**
```python
# Всегда проверяй контекст перед заполнением
if "«ПРЕДПРИЯТИЕ»" in paragraphs[22].text:
    # Это заголовок раздела с реквизитами предприятия
    # Параграфы 23-30 = реквизиты предприятия
```

### Проблема 2: Лишние подчеркивания остались

**Причина:** Неполная замена строки

**Решение:**
```python
# ❌ Плохо
text.replace('в размере: ___________', value)

# ✅ Хорошо
text.replace('в размере: ___________________________________________________________ не позднее',
             f'в размере: {value} не позднее')
```

### Проблема 3: Ошибка конвертации в PDF

**Причина:** unoconv не может прочитать файл

**Решение:**
```bash
# Проверь что файл существует и читаем
ls -lh contract.docx

# Проверь что unoconv работает
unoconv --version

# Пересохрани через python-docx
doc = Document('contract.docx')
doc.save('contract_final.docx')
unoconv -f pdf contract_final.docx
```

---

## 🔄 Workflow для сложных договоров

### Если договор нестандартный

```python
# 1. Читаем весь договор
full_analysis = analyze_contract_structure(doc)

# 2. Выводим структуру пользователю
print("Обнаружена следующая структура:")
for para_num, content_type in full_analysis.items():
    print(f"  [{para_num}] {content_type}")

# 3. Спрашиваем подтверждение
User: "Параграф 25 - это ИНН заказчика или исполнителя?"
Skill: "Заказчика (правая колонка)"

# 4. Заполняем с учетом уточнений
```

---

## 📚 References

- **companies.json** - база данных компаний
- **examples/** - примеры заполненных договоров
- **references/** - best practices и типовые структуры

---

## 🚀 Версия

**v1.0.0** - Initial release
- AI-анализ структуры договора
- Точечное редактирование параграфов
- Интерактивный сбор данных
- Автогенерация .docx + .pdf
- Поддержка любых типов договоров

---

## ⚖️ License

MIT License - свободное использование и модификация
