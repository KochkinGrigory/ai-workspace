# 🚀 AI Workspace - Инструкция по развертыванию

## 📦 Что это?

AI Workspace - готовый к продаже продукт для развертывания персонального AI агента на Linux серверах клиентов.

---

## 🌐 Доступ к файлам

### Страница загрузки
**URL:** https://my-jarvis.ru/downloads/

Красивая страница с инструкциями и кнопками для скачивания.

### Прямые ссылки

**Архив продукта:**
```
https://my-jarvis.ru/downloads/ai-workspace-v1.0.5.tar.gz
```
Размер: 241 KB

**Скрипт быстрой установки:**
```
https://my-jarvis.ru/downloads/install-ai-workspace.sh
```

---

## 🎯 Три способа установки для клиента

### Способ 1: Одна команда (САМЫЙ ПРОСТОЙ)

Просто дайте клиенту эту команду:

```bash
wget -O- https://my-jarvis.ru/downloads/install-ai-workspace.sh | sudo bash
```

Или через curl:
```bash
curl -sSL https://my-jarvis.ru/downloads/install-ai-workspace.sh | sudo bash
```

**Что происходит:**
1. Скачивается архив (240 KB)
2. Распаковывается во временную папку
3. Автоматически запускается install.sh
4. Клиент вводит токены в интерактивном режиме
5. Всё устанавливается автоматически
6. Клиент выполняет `sudo -u claude-agent claude login` для авторизации
7. Временные файлы удаляются

**Время:** 3-7 минут + авторизация

---

### Способ 2: Ручная установка

Для клиентов которые хотят контроля:

```bash
# 1. Скачать архив
wget https://my-jarvis.ru/downloads/ai-workspace-v1.0.5.tar.gz

# 2. Распаковать
tar -xzf ai-workspace-v1.0.5.tar.gz

# 3. Перейти в директорию
cd ai-workspace-v1.0.5/

# 4. Запустить установку
sudo ./install.sh

# 5. Авторизоваться в Claude
sudo -u claude-agent claude login
```

**Время:** 5-10 минут

---

### Способ 3: Ты устанавливаешь сам

Когда ты подключаешься к серверу клиента:

```bash
# 1. SSH на сервер клиента
ssh user@client-server

# 2. Скачать и установить одной командой
wget -O- https://my-jarvis.ru/downloads/install-ai-workspace.sh | sudo bash

# 3. Ввести токены клиента (должны быть заранее подготовлены)
# 4. Проверить работу
# 5. Показать клиенту как пользоваться
```

---

## 📋 Что нужно подготовить ПЕРЕД установкой

### От клиента:

1. **Claude Account**
   - Аккаунт на claude.ai с подпиской Claude Code
   - Для авторизации через `claude login` (OAuth)

2. **Telegram Bot Token**
   - Получить у @BotFather в Telegram
   - Формат: `1234567890:ABCdefGHI...`

3. **Telegram Chat ID**
   - Получить у @userinfobot
   - Формат: `123456789` (только цифры)

4. **Сервер:**
   - Ubuntu 20.04+ или Debian 11+
   - Минимум 2GB RAM
   - Минимум 10GB свободного места
   - SSH доступ с правами sudo

5. **Опционально:**
   - OpenAI API Key (для голосовых сообщений)
   - Gemini API Key (для генерации изображений)

---

## 🎬 Пошаговый процесс установки

### Шаг 1: Проверка системы (30 сек)
```
✅ ОС: Ubuntu 22.04
✅ RAM: 4096MB
✅ Свободное место: 50GB
✅ Порт 8081 свободен
```

### Шаг 2: Установка зависимостей (2-5 мин)
```
Docker + Docker Compose
Node.js 20.x
Claude CLI
```

### Шаг 3: Создание пользователя (10 сек)
```
claude-agent (UID=5001, GID=5001)
Группы: docker
Sudo: полный доступ
```

### Шаг 4: Ввод токенов (интерактивно)
```
Telegram Bot Token: _________
Telegram Chat ID: _________
OpenAI API Key: _________ (Enter чтобы пропустить)
Gemini API Key: _________ (Enter чтобы пропустить)
```

### Шаг 5: Установка файлов (30 сек)
```
Копирование в /opt/ai-workspace
Генерация .env
```

### Шаг 6: Сборка Docker (1-3 мин)
```
Building образа...
Запуск контейнера...
```

### Шаг 7: Проверка (5 сек)
```
✅ Контейнер запущен
✅ HTTP API отвечает
```

### Шаг 8: Авторизация Claude (автоматически)

После проверки работоспособности скрипт **автоматически спросит**:

```
Хотите авторизоваться в Claude Code сейчас? (y/n):
```

**Если выбрать `y`:**
- Запустится `sudo -u claude-agent claude login`
- Откроется URL для входа в аккаунт Claude
- Скопируйте URL в браузер
- Авторизуйтесь через свой Claude аккаунт (OAuth)
- Готово!

**Если выбрать `n`:**
- Авторизация пропускается
- Можно запустить позже: `sudo -u claude-agent claude login`

**Важно:** Claude Code работает через подписку Claude Code на вашем аккаунте, а не через API ключ Anthropic.

### Итого: 3-7 минут полная установка + авторизация (все в одном процессе)

---

## ✅ Проверка работоспособности

После установки:

### 1. Проверить статус контейнера
```bash
cd /opt/ai-workspace/apps/telegram-bot
docker compose ps
```

Должно быть: `Up (healthy)`

### 2. Проверить логи
```bash
docker logs ai-workspace-telegram-bot --tail 20
```

Не должно быть ошибок про токен или API.

### 3. Протестировать в Telegram

Попросить клиента:
1. Открыть Telegram
2. Найти бота (по username или токену)
3. Отправить `/start`
4. Попробовать простой запрос: "Привет, расскажи о себе"

---

## 🛠️ Управление после установки

### Полезные команды для клиента:

**Проверить статус:**
```bash
cd /opt/ai-workspace/apps/telegram-bot && docker compose ps
```

**Посмотреть логи:**
```bash
docker logs ai-workspace-telegram-bot -f
```

**Перезапустить:**
```bash
cd /opt/ai-workspace/apps/telegram-bot && docker compose restart
```

**Остановить:**
```bash
cd /opt/ai-workspace/apps/telegram-bot && docker compose down
```

**Запустить:**
```bash
cd /opt/ai-workspace/apps/telegram-bot && docker compose up -d
```

**Управление sudo правами:**
```bash
# Показать текущие права
sudo /opt/ai-workspace/scripts/show-sudo-permissions.sh

# Переключить режим (full/restricted)
sudo /opt/ai-workspace/scripts/set-sudo-mode.sh full

# Добавить команду в список
sudo /opt/ai-workspace/scripts/add-sudo-command.sh /usr/bin/certbot
```

**Полное удаление:**
```bash
sudo /opt/ai-workspace/uninstall.sh
```

---

## 📊 Мониторинг

### Проверить что бот работает:
```bash
# HTTP API должен отвечать
curl http://localhost:8081/health

# Контейнер должен быть Up
docker ps | grep telegram-bot
```

### Автозапуск при перезагрузке сервера:

Docker контейнер настроен с `restart: unless-stopped` - будет автоматически запускаться при перезагрузке сервера.

---

## 🔧 Troubleshooting

### Проблема: "Порт 8081 занят"
```bash
# Найти что использует порт
sudo lsof -i :8081

# Остановить конфликтующий процесс
sudo kill <PID>
```

### Проблема: "Docker daemon not running"
```bash
# Запустить Docker
sudo systemctl start docker

# Включить автозапуск
sudo systemctl enable docker
```

### Проблема: "Permission denied"
```bash
# Проверить что claude-agent в группе docker
id claude-agent

# Добавить если нет
sudo usermod -aG docker claude-agent
```

### Проблема: Бот не отвечает в Telegram
```bash
# 1. Проверить логи
docker logs ai-workspace-telegram-bot --tail 50

# 2. Проверить токен в .env
cat /opt/ai-workspace/apps/telegram-bot/.env | grep TOKEN

# 3. Проверить что контейнер запущен
docker ps | grep telegram-bot

# 4. Перезапустить
cd /opt/ai-workspace/apps/telegram-bot && docker compose restart
```

---

## 📦 Обновление архива на сервере

Когда нужно выложить новую версию:

```bash
# 1. Перейти в product директорию
cd /opt/ai-workspace-product/

# 2. Создать новый архив
tar -czf ai-workspace-v1.1.0.tar.gz \
    --exclude='*.tar.gz' \
    --exclude='*.log' \
    --exclude='.git' \
    --exclude='__pycache__' \
    .

# 3. Скопировать в downloads
sudo cp ai-workspace-v1.1.0.tar.gz /var/www/downloads/

# 4. Установить права
sudo chmod 644 /var/www/downloads/ai-workspace-v1.1.0.tar.gz
sudo chown www-data:www-data /var/www/downloads/ai-workspace-v1.1.0.tar.gz

# 5. Обновить ссылки в index.html и install-ai-workspace.sh
```

---

## 💰 Ценообразование (для справки)

При продаже учитывай:

**Что ВКЛЮЧЕНО в стоимость:**
- ✅ Сам AI Workspace (установка, настройка)
- ✅ 5 базовых skills
- ✅ Telegram бот
- ✅ Документация
- ✅ Техподдержка (по тарифу)

**Что клиент оплачивает ОТДЕЛЬНО:**
- ❌ VPS сервер (от 500₽/мес)
- ❌ Claude Code подписка (на claude.ai, ~$20/мес)
- ❌ OpenAI API (опционально, для голосовых)
- ❌ Gemini API (опционально, для генерации изображений)

---

## 📞 Поддержка

Если у клиента проблемы:
1. Проверь логи: `docker logs ai-workspace-telegram-bot`
2. Проверь статус: `docker ps`
3. Если нужно - подключись к серверу и диагностируй
4. Редкие проблемы - можно переустановить (3-5 минут)

---

**Готово!** Теперь у тебя есть полностью автоматизированный процесс продажи и развертывания AI Workspace 🚀
