# 🤖 AI Workspace Telegram Bot

Двусторонняя интеграция Telegram с ai-workspace для отправки уведомлений и приема команд.

## 📖 Документация

- **[DEPLOYMENT.md](./DEPLOYMENT.md)** - Полное руководство по установке на новом сервере (с нуля)
- **[SETUP.md](./SETUP.md)** - Быстрая настройка токенов (бот уже установлен)
- **[QUICKSTART.md](./QUICKSTART.md)** - Шпаргалка по использованию

## 🎯 Возможности

### Прием сообщений (от тебя → Claude)
- ✅ Принимает текстовые сообщения
- ✅ Принимает фото и документы
- ✅ Логирует всю историю общения
- ✅ Команды: `/start`, `/status`, `/help`

### Отправка сообщений (от Claude → тебе)
- ✅ HTTP API для отправки текста
- ✅ HTTP API для отправки файлов
- ✅ Поддержка HTML/Markdown форматирования
- ✅ Health check endpoint

## 🚀 Быстрый старт

### 1. Создай бота

1. Открой Telegram, найди **@BotFather**
2. Отправь `/newbot`
3. Назови бота (например: "My AI Workspace Bot")
4. Придумай username (например: `my_workspace_bot`)
5. Получишь **токен** вида: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### 2. Узнай свой chat_id

**Вариант A** (через @userinfobot):
1. Найди в Telegram: **@userinfobot**
2. Отправь `/start`
3. Скопируй свой ID

**Вариант B** (через API):
1. Напиши любое сообщение своему боту
2. Открой в браузере: `https://api.telegram.org/bot<TOKEN>/getUpdates`
3. Найди `"chat":{"id":123456789}`

### 3. Настрой конфигурацию

Добавь в `/opt/ai-workspace/.env`:

```bash
# Telegram Bot
TELEGRAM_BOT_TOKEN=твой_токен_от_BotFather
TELEGRAM_CHAT_ID=твой_chat_id
BOT_HTTP_PORT=8080
```

### 4. Запусти бота

```bash
cd /opt/ai-workspace/apps/telegram-bot
docker-compose up -d
```

### 5. Проверь статус

```bash
# Проверка контейнера
docker ps | grep telegram-bot

# Просмотр логов
docker logs -f ai-workspace-telegram-bot

# Health check
curl http://localhost:8080/health
```

## 📡 HTTP API

### Отправить текстовое сообщение

```bash
curl -X POST http://localhost:8081/send \
  -H "Content-Type: application/json" \
  -d '{"text": "Привет из Claude!", "parse_mode": "HTML"}'
```

### Отправить файл

```bash
curl -X POST http://localhost:8081/send_file \
  -F "file=@/path/to/report.xlsx" \
  -F "caption=Отчет по продажам"
```

### Health Check

```bash
curl http://localhost:8081/health
```

## 🛠️ Helper скрипт

Используй удобный скрипт из `/opt/ai-workspace/commands/tg-send.sh`:

```bash
# Отправить текст
/opt/ai-workspace/commands/tg-send.sh "Анализ завершён!"

# Отправить файл
/opt/ai-workspace/commands/tg-send.sh --file report.xlsx "Отчёт готов"

# С HTML форматированием
/opt/ai-workspace/commands/tg-send.sh "<b>Важно!</b> Проверь результаты"
```

## 📂 Структура проекта

```
telegram-bot/
├── Dockerfile              # Docker образ
├── docker-compose.yml      # Композиция сервисов
├── requirements.txt        # Python зависимости
├── README.md              # Эта документация
├── .env.example           # Пример конфигурации
├── src/
│   └── bot.py            # Основной код бота
└── logs/
    └── bot_*.log         # Логи (ротация каждый день)
```

## 🔐 Безопасность

- ✅ Проверка `ALLOWED_CHAT_ID` для всех входящих сообщений
- ✅ Только авторизованный пользователь может писать боту
- ✅ HTTP API доступен только локально (127.0.0.1:8080)
- ✅ Токен хранится в `.env` (не коммитится в git)

## 📝 Логирование

Логи сохраняются в `./logs/bot_YYYY-MM-DD.log`:
- Автоматическая ротация каждый день
- Хранение 30 дней
- Уровни: DEBUG, INFO, WARNING, ERROR

## 🔄 Управление

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Просмотр логов
docker-compose logs -f

# Пересборка после изменений
docker-compose up -d --build
```

## 🐛 Troubleshooting

### Бот не отвечает

```bash
# Проверь логи
docker logs ai-workspace-telegram-bot

# Проверь токен
docker exec ai-workspace-telegram-bot env | grep TELEGRAM
```

### HTTP API недоступен

```bash
# Проверь порт
netstat -tlnp | grep 8080

# Проверь health
curl http://localhost:8080/health
```

### Ошибка "Unauthorized"

- Проверь токен в `.env`
- Убедись, что токен скопирован полностью
- Попробуй создать нового бота

## 🎨 Будущие улучшения

- [ ] Webhook режим (вместо long polling)
- [ ] Интеграция с MCP для прямой связи с Claude
- [ ] Команды для запуска анализа БД
- [ ] Отправка графиков и визуализаций
- [ ] Планировщик задач (cron-like)
- [ ] Inline клавиатуры для интерактивности

## 📚 Технологии

- **Python 3.11** - Язык программирования
- **aiogram 3.14** - Асинхронная библиотека для Telegram Bot API
- **aiohttp** - HTTP сервер и клиент
- **loguru** - Красивое логирование
- **Docker** - Контейнеризация

## 📄 Лицензия

Proprietary - часть ai-workspace проекта
