# 🎨 HTML Форматирование для Telegram - Шпаргалка

**⚠️ КРИТИЧНО:** Telegram использует `parse_mode="HTML"` - используй ТОЛЬКО HTML теги!

## Основные теги

| Что нужно | HTML тег | Пример |
|-----------|----------|--------|
| **Жирный** | `<b>текст</b>` | `<b>Важно!</b>` |
| *Курсив* | `<i>текст</i>` | `<i>Примечание</i>` |
| `Код` | `<code>текст</code>` | `<code>123</code>` |
| Блок кода | `<pre>код</pre>` | `<pre>SELECT * FROM...</pre>` |
| Ссылка | `<a href="url">текст</a>` | `<a href="https://google.com">Google</a>` |
| Список | `•` (символ bullet) | `• Первый пункт` |

## ❌ НЕ используй Markdown!

```
**жирный**    ❌ → <b>жирный</b>    ✅
*курсив*      ❌ → <i>курсив</i>    ✅
`код`         ❌ → <code>код</code> ✅
- список      ❌ → • список         ✅
```

## Примеры

### ✅ Правильно
```html
<b>Анализ завершен!</b>

Результаты за октябрь 2024:  ← дата в обычном тексте
• Продаж: <code>1,234</code>  ← конкретное значение
• Выручка: <code>5,678,900 ₽</code>  ← данные из БД
• Конверсия: <b>23.4%</b>

<i>Нашел 3 проблемы в воронке</i>  ← "3" в тексте без <code>
```

### ❌ Неправильно
```
**Анализ завершен!**  ← пользователь увидит звездочки!

Результаты:
- Продаж: 1,234  ← тире вместо bullet
- Выручка: 5,678,900 ₽  ← данные не выделены
- Конверсия: 23.4%

*Данные актуальны на сегодня*  ← звездочки видны!
```

## Специальные символы

Если нужно вставить `<`, `>`, `&` в текст:
- `<` → `&lt;`
- `>` → `&gt;`
- `&` → `&amp;`

Пример:
```html
Формула: <code>x &lt; 10 &amp;&amp; y &gt; 5</code>
```

## Эмодзи

Используй эмодзи для структуры:
- ✅ Успех
- ❌ Ошибка
- ⚠️ Предупреждение
- 📊 Данные/статистика
- 📝 Текст/документ
- 🔍 Поиск/анализ
- 💾 Файл/сохранение
- ⏳ Процесс выполняется
- 🎉 Завершение

## Золотое правило

**Когда использовать `<code>`:**
- ✅ Конкретные значения из БД: `Продаж: <code>1,234</code>`
- ✅ Технические данные: `Порт <code>8081</code>`, `ID <code>#12345</code>`
- ✅ Код и пути: `<code>/path/to/file</code>`
- ❌ НЕ для чисел в тексте: `"Нашел 3 проблемы"` (без `<code>`)
- ❌ НЕ для дат: `"за октябрь 2024"` (без `<code>`)

**Всегда оборачивай:**
- Важные слова → `<b>слово</b>`
- Конкретные данные → `<code>значение</code>` (только где логично!)
- Примечания → `<i>текст</i>`
- Списки → `•` (не `-`)
