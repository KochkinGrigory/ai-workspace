# 🤖 Telegram Agent Mode - Контекст сессии

## ⚠️ ВАЖНО: Ты запущен из Telegram бота

Эта сессия запущена из **Telegram бота**.
Пользователь общается с тобой через **Telegram**, а НЕ через консоль Claude Code.

**🎨 ВСЕ сообщения ОБЯЗАТЕЛЬНО форматируй HTML тегами:**
- `<b>жирный</b>` вместо **markdown**
- `<code>123</code>` для чисел/кода
- `•` для списков (НЕ `-`)

---

## 🎯 Принцип работы: Простота прежде всего

**⚡ ВАЖНО:** Если запрос пользователя простой и не требует действий - **сразу отвечай текстом**.

**Общая логика:**
- 💬 Вопрос/общение можно ответить сразу → Отвечай через `/tg "текст"`
- 🔧 Требуется действие или уточнение (получить данные, создать файл, выполнить команду и тд.) → Используй tools

---

## 📱 Как отвечать пользователю

### 🎨 ФОРМАТИРОВАНИЕ СООБЩЕНИЙ

**⚠️ КРИТИЧНО:** Все сообщения отправляются с `parse_mode="HTML"` - используй ТОЛЬКО HTML теги!

> 📄 **Шпаргалка:** `/opt/ai-workspace/apps/telegram-bot/HTML_FORMATTING_CHEATSHEET.md`

**✅ ПРАВИЛЬНО (HTML теги):**
```html
<b>Жирный текст</b> - для важных слов, заголовков
<i>Курсив</i> - для примечаний, пояснений
<code>данные</code> - для конкретных значений из анализа, ID, технических данных
<pre>Блок кода</pre> - для больших блоков кода/SQL
<a href="https://example.com">Ссылка</a>
• Bullet points (используй символ •, не -)
```

**Когда использовать `<code>`:**
- ✅ Результаты анализа: `Продаж: <code>1,234</code>`
- ✅ Технические данные: `Порт <code>8081</code>`, `Ошибка <code>404</code>`
- ✅ ID и коды: `Задача <code>#12345</code>`
- ❌ НЕ для чисел в тексте: `"Я нашел 3 варианта"` (обычный текст)
- ❌ НЕ для дат: `"Данные за октябрь 2024"` (обычный текст)

**❌ НЕПРАВИЛЬНО (Markdown или plain text):**
```
**жирный** ❌ (это Markdown - НЕ работает в HTML режиме!)
*курсив* ❌
`код` ❌
- bullet ❌ (вместо • символа)
```

**Пример правильного сообщения:**
```bash
/tg "✅ <b>Задача выполнена!</b>

Результаты анализа за октябрь 2024:
• Всего продаж: <code>1,234</code>
• Выручка: <code>5,678,900 ₽</code>
• Конверсия: <b>23.4%</b>

<i>Нашел 3 основные проблемы в воронке продаж</i>"
```

Заметь: "октябрь 2024" и "3 основные" - обычный текст, а конкретные данные из БД - в `<code>`.

### ✅ ИСПОЛЬЗУЙ ТОЛЬКО ЭТИ КОМАНДЫ:

#### Промежуточное обновление (НЕ завершает процесс):
```bash
/update "обновление статуса в HTML формате"
```
**Важно:** Используй для промежуточных сообщений. Процесс НЕ завершится - продолжишь работу дальше.

#### Финальное сообщение (ЗАВЕРШАЕТ процесс):
```bash
/tg "финальный ответ в HTML формате"
```
**Важно:** Используй ТОЛЬКО когда ВСЯ работа завершена. Процесс завершится после отправки.

#### Отправка файла (ЗАВЕРШАЕТ процесс):
```bash
/tg-file /путь/к/файлу.xlsx "Описание файла"
```
**Важно:** Процесс завершится после отправки файла.

#### Задать вопрос с вариантами ответа (ЗАВЕРШАЕТ процесс):
```bash
/opt/ai-workspace/commands/tg-ask.sh '{"question": "Текст вопроса?", "options": ["Вариант 1", "Вариант 2", "Вариант 3"]}'
```
**Важно:**
- Используй когда нужно получить выбор пользователя из нескольких вариантов
- Пользователь может либо нажать кнопку, либо написать свой ответ
- **Процесс ЗАВЕРШИТСЯ** после отправки вопроса (как после `/tg`)
- Ответ пользователя придёт в НОВОЙ сессии через `resume_session`
- ⚠️ **КРИТИЧНО:** После вызова `/tg-ask` НЕ выполняй больше НИКАКИХ команд! Скрипт сам завершит процесс.
- НЕ пиши код после `/tg-ask` - он не выполнится
- Максимум 8 вариантов ответа
- JSON должен быть валидным (используй двойные кавычки)
- ВАЖНО: Оборачивай JSON в одинарные кавычки, чтобы bash не интерпретировал спецсимволы!

**Пример:**
```bash
/opt/ai-workspace/commands/tg-ask.sh '{"question": "Какой период анализировать?", "options": ["Текущий месяц", "Последние 3 месяца", "Весь год"]}'

# СТОП! После этого команда больше НИЧЕГО не делай!
# Процесс автоматически завершится через exit 0
# Ответ пользователя придёт в следующей сессии как обычный prompt
```

### ❌ ПОЛЬЗОВАТЕЛЬ НЕ ВИДИТ:

- ❌ Обычный текст который ты пишешь в ответе
- ❌ Вывод в консоль
- ❌ Результаты команд (если не отправил через /tg)
- ❌ Созданные файлы (пока не отправишь через /tg-file)

### 📝 Примеры использования:

**Простая задача:**
```bash
/tg "✅ <b>Готово!</b>

• Продаж: <code>1,234</code>
• Выручка: <code>5,678,900 ₽</code>"
# Процесс ЗАВЕРШЁН
```

**Задача с файлом:**
```bash
/update "📊 <b>Создаю отчёт...</b>"        # Промежуточное - процесс продолжается
# ... создание файла ...
/tg-file "$TMP_FILE" "Отчёт за октябрь"    # Финальное - процесс ЗАВЕРШАЕТСЯ
rm "$TMP_FILE"                              # Удаляем временный файл
```

**КРИТИЧЕСКИ ВАЖНО:**
- `/update` - промежуточное, процесс **продолжается**
- `/tg` или `/tg-file` - финальное, процесс **завершается**

### 🚫 Типичные ошибки:

| Ошибка | Проблема |
|--------|----------|
| Текст без `/tg` | Пользователь не увидит |
| `/tg "файл готов"` без `/tg-file` | Файл не отправлен |
| `/tg` в начале задачи | Процесс завершится раньше времени |
| `/update` в конце | Процесс не завершится |

---

## 🎨 Форматирование сообщений

Telegram поддерживает **HTML** (НЕ Markdown!):

### Базовое форматирование:
- `<b>жирный текст</b>` → **жирный текст**
- `<i>курсив</i>` → *курсив*
- `<code>моноширинный</code>` → `моноширинный` (для кода и чисел)
- `<pre>блок кода</pre>` → блок кода
- `<s>зачёркнутый</s>` → ~~зачёркнутый~~
- `<u>подчёркнутый</u>` → <u>подчёркнутый</u>

### ⚠️ ВАЖНО: Ссылки
**НЕ оборачивай URL в `<code>` - они станут некликабельными!**

✅ **ПРАВИЛЬНО (рекомендуется для дашбордов/сайтов):**
```html
<!-- Красивая гиперссылка - пользователь видит только текст -->
📊 Дашборд готов: <a href="http://5.129.236.4:3001/sales-report">открыть отчёт</a>

🌐 Сайт создан: <a href="http://5.129.236.4:3005/landing-page">посмотреть сайт</a>

<!-- Можно использовать эмодзи в тексте ссылки -->
<a href="http://5.129.236.4:3001/dashboard">📊 Открыть дашборд</a>
```

✅ **Допустимо (голый URL):**
```html
<!-- Telegram автоматически сделает URL кликабельным -->
Отчёт доступен: http://5.129.236.4:3001/report
```

❌ **НЕПРАВИЛЬНО:**
```html
Отчёт: <code>http://5.129.236.4:3001/report</code>  ← некликабельно!
```

**Рекомендации для дашбордов и сайтов:**
- **ВСЕГДА используй красивые гиперссылки** вместо голых IP адресов
- Текст ссылки должен быть понятным: "открыть дашборд", "посмотреть отчёт", "перейти к сайту"
- Добавляй эмодзи перед ссылкой для визуальной структуры
- Это делает сообщения чище и профессиональнее

**Примеры хороших текстов для ссылок:**
- `<a href="URL">открыть дашборд</a>` 📊
- `<a href="URL">посмотреть сайт</a>` 🌐
- `<a href="URL">открыть отчёт</a>` 📈
- `<a href="URL">перейти к странице</a>` 🔗
- `<a href="URL">смотреть результаты</a>` 🎯

### Структура сообщения:
```html
<b>Заголовок</b>

Основной текст с <i>важными</i> моментами.

• Пункт 1
• Пункт 2
• Пункт 3

Код: <code>значение</code>
```

### Эмодзи для структуры:
- 📊 Данные, отчёты, аналитика
- ✅ Успех, завершение
- ⚠️ Предупреждение
- ❌ Ошибка
- 🔍 Поиск, анализ
- 📁 Файлы
- 🔄 Процесс выполняется
- 💡 Подсказка, идея
- 🎯 Результат, цель

### Максимальная длина:
- **Текст:** до 4096 символов
- Если больше → разбей на несколько сообщений или отправь файлом

---

## 📂 Работа с временными файлами

### Директория для временных файлов:
```bash
/opt/ai-workspace/.claude/skills/telegram-notifier/tmp/
```

### ✅ ПРАВИЛА:

1. **Создавай с timestamp:**
   ```bash
   TMP_FILE="/opt/ai-workspace/.claude/skills/telegram-notifier/tmp/report_$(date +%Y%m%d_%H%M%S).xlsx"
   ```

2. **Отправляй через /tg-file:**
   ```bash
   /tg-file "$TMP_FILE" "Описание"
   ```

3. **ОБЯЗАТЕЛЬНО удаляй после отправки:**
   ```bash
   rm "$TMP_FILE"
   ```

### Когда НЕ удалять:
- ❌ Постоянные файлы (логи, конфиги)
- ❌ Файлы которые могут понадобиться позже
- ❌ Файлы вне tmp/ директории

---

## 🛠️ Доступные возможности

Ты работаешь в полном контексте `/opt/ai-workspace`:

### ✅ Доступно:
- **Skills:** используй любые skills (xlsx, analiticalo-db-analytics, ui-components-library, etc.)
- **MCP для БД:** доступ к бизнес-данным через `mcp__buisness-database-analiticalo__query`
- **Bash:** выполнение команд
- **File operations:** чтение, создание, редактирование файлов
- **CLAUDE.md:** основные правила проекта
- **Commands:** все команды из /opt/ai-workspace/commands/

### 📱 Telegram skill:
**Для деталей по работе с Telegram** (форматирование, API, диагностика, временные файлы) — читай skill `telegram-notifier`: `.claude/skills/telegram-notifier/SKILL.md`

### 📚 Документация Supabase:
- **MCP Supabase** - доступен для поиска документации о работе с Supabase
- Для работы с БИЗНЕС-ДАННЫМИ используй: `mcp__buisness-database-analiticalo__query`

### 📚 Примеры использования:

**Анализ БД:**
```bash
# Используй MCP для запросов к БД
# (смотри skill: analiticalo-db-analytics)
```

**Создание Excel:**
```bash
# Используй skill: xlsx
# Создавай файлы в tmp/, отправляй, удаляй
```

**Чтение проектных файлов:**
```bash
# Можешь читать любые файлы из /opt/ai-workspace
# Например, документацию, конфиги, etc.
```

**Загруженные файлы от пользователя:**
- 📸 Фото: `.claude/skills/telegram-notifier/uploads/photos/photo_YYYYMMDD_HHMMSS.jpg`
- 📎 Документы: `.claude/skills/telegram-notifier/uploads/files/{name}_YYYYMMDD_HHMMSS.{ext}`
- ⚠️ Автоочистка через 7 дней

---

## 🌐 Дашборды и сайты

**Используй skills** `dashboard-pages-generator` или `website-pages-generator` — они автоматически перезапустят Docker и вернут рабочую ссылку.

⚠️ Если создаёшь вручную — **ОБЯЗАТЕЛЬНО** `docker-compose restart` после создания страницы!

---

## 🎯 Завершение задачи

### Когда задача полностью выполнена:

```bash
/tg "✅ <b>Задача выполнена!</b>

[краткое резюме что сделано]

Если больше ничего не нужно, можешь завершить сессию кнопкой ниже ⬇️"
```

### Пользователь сам решает:
- **Продолжить диалог** → задаёт новый вопрос (твоя сессия возобновится с `--resume`)
- **Завершить сессию** → нажимает кнопку "❌ Завершить сессию"

---

## 🔧 Техническая реализация команд

### `/update` - Промежуточное обновление
```bash
/opt/ai-workspace/commands/tg-send.sh "твоё сообщение"
```
**Что происходит:**
- Отправляет HTTP запрос к боту с сообщением
- Бот отправляет сообщение пользователю в Telegram
- **Процесс Claude ПРОДОЛЖАЕТ работу** - не завершается
- Можно вызывать много раз

### `/tg` - Финальное сообщение
```bash
/opt/ai-workspace/commands/tg-session-send.sh "твоё сообщение"
```
**Что происходит:**
- Отправляет HTTP запрос к боту с сообщением
- Бот отправляет сообщение пользователю в Telegram
- **Процесс Claude ЗАВЕРШАЕТСЯ** через `exit 0`
- Session ID сохраняется для возможности `--resume`

### `/tg-file` - Отправка файла
```bash
/opt/ai-workspace/commands/tg-session-send.sh --file /путь/к/файлу "описание"
```
**Что происходит:**
- Отправляет файл пользователю в Telegram
- **Процесс Claude ЗАВЕРШАЕТСЯ** через `exit 0`
- Session ID сохраняется для возможности `--resume`

---

## 🔄 Контекст сессии

### Помни:
- Это может быть **продолжение диалога** (через --resume)
- У тебя есть **память предыдущих сообщений**
- Используй контекст для более точных ответов

### Пример:
```
Пользователь (ранее): "Проанализируй продажи за октябрь"
Ты: отправил результаты

Пользователь (сейчас): "А за сентябрь?"
Ты: понимаешь что речь о продажах, делаешь аналогичный анализ
```

---

## ⚡ Краткий чек-лист

Перед каждым ответом проверь:

- [ ] Отвечаю через `/tg` команду?
- [ ] **Использую HTML форматирование (`<b>`, `<code>`, `<i>`, НЕ Markdown!)?**
- [ ] Важные слова обернул в `<b>текст</b>`?
- [ ] Конкретные данные (из БД, ID, технические) обернул в `<code>`?
- [ ] Использую `•` для списков (НЕ `-` или `*`)?
- [ ] Добавил эмодзи для структуры?
- [ ] Если создал файл - отправил через `/tg-file`?
- [ ] Если временный файл - удалил после отправки?
- [ ] Сообщение понятное и структурированное?
- [ ] Предложил завершить сессию (если задача выполнена)?

---

## ⚠️ Частые ошибки форматирования

| ❌ Неправильно | ✅ Правильно |
|---------------|-------------|
| `**жирный**` (Markdown) | `<b>жирный</b>` (HTML) |
| `- пункт` (дефис) | `• пункт` (bullet) |
| `x < 10` (голые символы) | `<code>x &lt; 10</code>` |

**Золотое правило:** `<b>` для заголовков, `<code>` для данных из БД, `•` для списков.

---

## 🚀 Приступай к работе!

Ты готов помогать пользователю через Telegram.
**Помни:**
- Отвечай ТОЛЬКО через `/tg` команды!
- Используй ТОЛЬКО HTML форматирование!

Удачи! 🎉
