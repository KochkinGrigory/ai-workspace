# Dashboard Pages Generator

Система автоматической генерации интерактивных дашбордов с визуализацией данных.

## Обзор

Проект для создания отдельных веб-страниц с дашбордами, которые содержат графики, таблицы и метрики. Каждый дашборд - это независимая страница, которую можно создать, показать пользователю и удалить.

## Технологии

- **Next.js 15** (App Router)
- **TypeScript**
- **Tailwind CSS** (mobile-first, адаптивный дизайн)
- **Recharts** (графики)
- **shadcn UI** (компоненты)
- **Docker** (production deployment)

## Адаптивность

Все дашборды **ОБЯЗАТЕЛЬНО** создаются с поддержкой мобильных устройств:

- 📱 Mobile-first подход
- Tailwind CSS breakpoints (sm, md, lg, xl)
- ResponsiveContainer для всех графиков
- Адаптивные grid layouts
- Горизонтальная прокрутка таблиц на узких экранах

## Структура проекта

```
dashboard-pages/
├── frontend/
│   ├── src/
│   │   ├── app/
│   │   │   ├── layout.tsx           # Root layout
│   │   │   ├── globals.css          # Стили Tailwind
│   │   │   ├── october-2025/        # Пример дашборда
│   │   │   │   └── page.tsx
│   │   │   └── [report-name]/       # Другие дашборды
│   │   │       └── page.tsx
│   │   ├── components/ui/
│   │   │   └── card.tsx             # UI компоненты
│   │   └── lib/
│   │       └── utils.ts             # Утилиты
│   ├── Dockerfile
│   ├── package.json
│   └── tsconfig.json
├── docker-compose.yml
└── README.md
```

## Использование

### Создание нового дашборда

1. Создайте папку в `frontend/src/app/[report-name]/`
2. Создайте файл `page.tsx` внутри папки
3. Используйте шаблон из skill документации
4. ⚠️ **ОБЯЗАТЕЛЬНО перезапустите Docker:**
   ```bash
   cd /opt/ai-workspace/apps/dashboard-pages
   docker-compose restart
   ```
   **БЕЗ ЭТОГО ШАГА ДАШБОРД НЕ БУДЕТ РАБОТАТЬ!**
5. Проверьте доступность: `curl -I http://5.129.236.4:3001/[report-name]`
6. Дашборд доступен по адресу: `http://5.129.236.4:3001/[report-name]`

### Удаление дашборда

```bash
rm -rf frontend/src/app/[report-name]
docker-compose restart
```

### Docker команды

```bash
# Перезапуск (после изменений в коде)
docker-compose restart

# Полный rebuild
docker-compose down
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f dashboard

# Статус контейнера
docker ps | grep dashboard-frontend
```

## Публичный доступ

- **IP:** 5.129.236.4
- **Порт:** 3001
- **URL шаблон:** http://5.129.236.4:3001/[report-name]

Пример: http://5.129.236.4:3001/october-2025

## Связанные skills

- **dashboard-pages-generator** - автоматизирует создание дашбордов
- **analiticalo-db-analytics** - получение данных из БД

## Важные правила

✅ Всегда работать через Docker
✅ Каждый отчет = отдельная папка
✅ Использовать kebab-case для имен
✅ Перезапускать Docker после изменений
✅ **ОБЯЗАТЕЛЬНО: Адаптивный дизайн под мобильные устройства**
  - Tailwind breakpoints: `grid-cols-1 md:grid-cols-2 lg:grid-cols-4`
  - ResponsiveContainer для всех графиков
  - Меньше отступы на mobile: `px-4 md:px-6`

❌ Не запускать npm напрямую
❌ Не создавать главную страницу (/)
❌ Не использовать API endpoints (данные hardcoded)
❌ Не забывать про mobile - проверяй на разных размерах экрана
