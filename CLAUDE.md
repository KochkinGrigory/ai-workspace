# AI Workspace - Инструкции для Claude Code

> Основные инструкции для работы Claude Code в AI Workspace

## Контекст окружения

**Рабочая директория:** `/opt/ai-workspace`
**Окружение:** Production Linux server с Docker
**Основное приложение:** Telegram бот для взаимодействия с пользователем

---

## Важные напоминания

### Общие правила

- Всегда анализируй доступные skills перед выполнением задачи
- Используй готовые skills вместо написания кода с нуля
- После создания/изменения файлов ОБЯЗАТЕЛЬНО перезапускай Docker контейнеры
- Не используй emojis если пользователь явно не просит
- Фокусируйся на решении задачи, а не на валидации идей пользователя

---

## Skills - Доступные возможности

Все skills находятся в `.claude/skills/`. Используй их автоматически при соответствующих запросах.

### 1. **xlsx** 📊
**Когда использовать:** Работа с Excel файлами

**Триггеры:**
- "создай таблицу Excel"
- "экспорт в xlsx"
- "прочитай Excel файл"
- "добавь формулы в таблицу"

**Возможности:**
- Создание таблиц с формулами и форматированием
- Чтение и анализ данных из XLSX
- Редактирование существующих файлов
- Условное форматирование, стили, цвета
- Автоматический пересчет формул

**Важно:** Skill автоматически использует openpyxl для формул и pandas для анализа

---

### 2. **nano-banana-image-gen** 🎨
**Когда использовать:** Генерация изображений по текстовому описанию

**Триггеры:**
- "создай изображение"
- "сгенерируй картинку"
- "нарисуй"
- "сделай иллюстрацию"

**Возможности:**
- Text-to-Image (генерация с нуля)
- Image-to-Image (редактирование существующих)
- Референсные изображения (до 14 штук)
- Multi-turn редактирование
- Разрешения: 1K, 2K, 4K

**Модель:** см. `scripts/config.py` в skill

**Методология:** Используй "5 столпов" - читай best practices в skill перед генерацией

---

### 3. **telegram-notifier** 📱
**Когда использовать:** Отправка уведомлений и файлов в Telegram

**Триггеры:**
- "отправь в телеграм"
- "notify telegram"
- "пришли файл пользователю"

**Команды:**
- `/tg [текст]` - отправить сообщение
- `/tg-file [путь]` - отправить файл
- `/tg-status` - проверить статус бота

**Важно:**
- Файлы для отправки сохраняются в `.claude/skills/telegram-notifier/uploads/`
- Автоочистка файлов старше 7 дней
- **Два режима работы:**
  - **Консоль Claude Code:** используй skill `telegram-notifier` → просто отправляет сообщение
  - **Telegram сессия** (видишь `telegram_agent_context.md`): используй `/update` (промежуточное) и `/tg` (финальное, **завершает процесс**)

---

### 4. **skill-creator** 🛠️
**Когда использовать:** Создание новых skills для расширения функционала

**Триггеры:**
- "создай skill для..."
- "добавь возможность..."
- "нужен новый функционал для..."

**Возможности:**
- Создание структуры нового skill
- Генерация SKILL.md с инструкциями
- Настройка зависимостей и конфигурации
- Тестирование нового skill

**Важно:** Используй skill-creator когда пользователю нужен функционал, которого нет в базовых skills

---

### 5. **mcp-integration** 🔌
**Когда использовать:** Подключение MCP серверов для интеграции с внешними сервисами

**Триггеры:**
- "подключи MCP сервер"
- "интеграция с базой данных"
- "настрой подключение к API"

**Возможности:**
- Подключение к базам данных (PostgreSQL, MySQL, MongoDB)
- Интеграция с CRM системами (Bitrix24, Salesforce, HubSpot)
- Подключение к REST/GraphQL API
- Настройка MCP конфигурации

**Важно:** MCP серверы дают Claude доступ к внешним данным и сервисам

---

## Приложения

### Telegram Bot (порт 8081)

**Путь:** `/opt/ai-workspace/apps/telegram-bot/`
**Технологии:** Python 3.11, aiogram, Docker
**Назначение:** Двусторонняя связь с пользователем через Telegram

**Docker:**
- Container: `ai-workspace-telegram-bot`
- Port: `8081:8081` (HTTP API)
- User: `claude-agent` (внутри контейнера)
- Status: Up (healthy)

**Команды управления:**
```bash
cd /opt/ai-workspace/apps/telegram-bot
docker-compose restart        # Перезапуск
docker-compose logs -f        # Логи
```

**Важно:** Бот запускает Claude сессии от пользователя `claude-agent` через sudo

---

## Slash Commands

Доступные команды в `.claude/commands/`:

- `/tg [текст]` - отправить сообщение в Telegram
- `/tg-file [путь]` - отправить файл в Telegram
- `/tg-ask` - задать вопрос с кнопками-вариантами ответа
- `/tg-status` - проверить статус Telegram бота

---

## Типичные рабочие процессы

### Workflow 1: Создание Excel отчета

```
Запрос → xlsx (создать таблицу)
      → /tg-file (отправить пользователю)
```

### Workflow 2: Генерация изображения

```
Запрос → nano-banana-image-gen (создать изображение)
      → /tg-file (отправить пользователю)
```

### Workflow 3: Добавление нового функционала

```
Запрос "Создай skill для работы с PDF"
      → skill-creator (создать новый skill)
      → Объяснить пользователю как использовать
```

### Workflow 4: Подключение к внешней системе

```
Запрос "Подключи мою PostgreSQL базу"
      → mcp-integration (настроить MCP сервер)
      → Проверить подключение
      → Показать примеры использования
```

---

## Расширяемость

Когда пользователю нужен функционал, которого нет:

### Вариант 1: Создать новый skill
```
Используй skill-creator для создания нового модуля
Примеры: PDF, email, web scraping, video processing
```

### Вариант 2: Подключить MCP сервер
```
Используй mcp-integration для подключения внешнего сервиса
Примеры: база данных, CRM, облачное хранилище, API
```

### Вариант 3: Предложить альтернативу
```
Если задачу можно решить существующими skills - предложи как
```

**Важно:** Всегда предлагай расширение вместо отказа "не могу это сделать"

---

## База данных

**По умолчанию:** Нет предустановленной БД
**Подключение:** Через mcp-integration skill

Если пользователь хочет работать с данными:
1. Предложи подключить MCP сервер для его БД
2. Используй mcp-integration для настройки
3. После подключения - доступ через MCP tools

---

## Документация

**Основные файлы:**
- `README.md` - Общая информация о продукте
- `CLAUDE.md` - Этот файл (инструкции для тебя)
- `.claude/skills/*/SKILL.md` - Документация каждого skill

**Telegram бот:**
- `apps/telegram-bot/README.md` - Описание возможностей
- `apps/telegram-bot/DEPLOYMENT.md` - Установка на новом сервере

---

## Ключевые правила

### ✅ ВСЕГДА:
- Использовать skills для соответствующих задач
- Перезапускать Docker после изменений в приложениях
- Предлагать расширение функционала (skill-creator, mcp-integration)
- Отправлять результаты в Telegram через `/tg` или `/tg-file`
- Давать конкретные инструкции вместо общих советов

### ❌ НИКОГДА:
- Не отказывай в задаче - предложи как расширить функционал
- Не создавай HTML или Python HTTP серверы
- Не коммитить .env файлы
- Не упоминай специфичные домены или серверы (универсальный продукт)
- Не используй emojis без запроса пользователя

---

## Безопасность

- Workspace: работает от `root` (для гибкости разработки)
- Telegram bot: Claude сессии запускаются от `claude-agent`
- Секреты: хранятся в `/opt/ai-workspace/.env`
- Docker: обеспечивает изоляцию процессов

---

## Архитектура Telegram бота

### Как работает бот:

```
Пользователь в Telegram
    ↓
Сообщение → Telegram Bot (Python/aiogram)
    ↓
Bot запускает: sudo -u claude-agent claude ...
    ↓
Claude Code сессия (в /opt/ai-workspace)
    ↓
Skills (при необходимости)
    ↓
Ответ → Bot → Telegram
```

### Важные детали:

1. **Пользователь claude-agent:**
   - UID: 994, GID: 986
   - Входит в группу docker
   - Имеет sudo права
   - Используется для запуска Claude сессий

2. **Управление сессиями:**
   - Бот сохраняет session_id для продолжения диалога
   - `/stop` - остановить выполнение, но сохранить сессию
   - `/restart` - полностью завершить и начать новую

3. **Поддерживаемые типы сообщений:**
   - Текст
   - Фото
   - Документы
   - Голосовые (требует OpenAI API для транскрипции)
   - Видео
   - Аудио

---

## Конфигурация

### Переменные окружения (.env):

```bash
# Обязательно
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHAT_ID=...

# Опционально
OPENAI_API_KEY=...  # Для голосовых сообщений
BOT_HTTP_PORT=8081
LOG_LEVEL=INFO
```

### Permissions (settings.local.json):

Разрешены основные команды:
- Docker, npm, node, git
- Python, pip
- Системные команды (ps, kill, systemctl)
- Все MCP tools (`mcp__*`)
- Все Skills и SlashCommands

---

## Работа с файлами

### Временные файлы:
- `.claude/skills/telegram-notifier/tmp/` - временные файлы (промпты, изображения)
- Автоочистка через cron job

### Загрузки от пользователя:
- `.claude/skills/telegram-notifier/uploads/photos/` - фото
- `.claude/skills/telegram-notifier/uploads/files/` - документы
- Автоочистка файлов старше 7 дней

---

## Troubleshooting

### Бот не отвечает:
```bash
docker logs ai-workspace-telegram-bot
docker ps | grep telegram-bot
docker-compose restart
```

### Claude сессии не запускаются:
```bash
# Проверь claude-agent
id claude-agent
sudo -u claude-agent claude --version

# Проверь Docker
docker ps
ls -la /var/run/docker.sock
```

### Файлы не отправляются:
```bash
# Проверь права доступа
ls -la .claude/skills/telegram-notifier/uploads/

# Проверь HTTP API
curl http://localhost:8081/health
```

---

## Расширение функционала

Для добавления новых возможностей:

1. **Новый skill:**
   ```
   Используй skill-creator
   Создается структура в .claude/skills/[name]/
   Документация в SKILL.md
   ```

2. **Slash команда:**
   ```
   Создай .claude/commands/[name].md
   Формат: markdown с инструкциями
   ```

3. **MCP сервер:**
   ```
   Используй mcp-integration
   Настраивается в mcp.json
   Доступ через mcp__[name]__* tools
   ```

4. **Helper скрипт:**
   ```
   Создай commands/[script].sh
   Делай исполняемым: chmod +x
   ```

---

## Позиционирование продукта

**AI Workspace** — минималистичный универсальный AI агент:

**Философия:**
- Даем базовый функционал из коробки (5 skills)
- Клиент расширяет под свои нужды
- Не перегружаем функциями, которые не всем нужны

**Ценность:**
- Быстрая установка (5 минут)
- Telegram интеграция (удобно)
- Легко расширяется (skill-creator, mcp-integration)
- Self-hosted (данные клиента на его сервере)

**Когда предлагать расширение:**
- Пользователю нужен PDF → "Создам skill для PDF"
- Нужна БД → "Подключим твою БД через MCP"
- Нужен email → "Создам skill для SMTP"

---

## Помни

Этот файл содержит инструкции для Claude Code. Для технической информации см. [README.md](./README.md)

**Цель:** Помогать пользователю максимально эффективно, предлагая расширение вместо ограничений.

---

**Версия:** 1.0.0 (универсальный продукт)
**Дата:** 2026-01-07
