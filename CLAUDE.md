# AI Workspace - Инструкции для Claude Code

> Основная рабочая директория для Claude Code на production сервере analiticalo.ru

## Контекст окружения

**Server:** 5.129.236.4 (production)
**Working Directory:** `/opt/ai-workspace`
**Environment:** Production Linux server с Docker, Nginx, Supabase

Для технических деталей (порты, контейнеры, установка) см. [README.md](./README.md)

---

## Важные напоминания

### 🔄 Превентивное улучшение (критично!)

**Когда возникает ошибка или проблема в работе:**
1. Проанализируй, может ли эта ошибка повториться в будущем
2. Если да - **ОБЯЗАТЕЛЬНО предложи обновить документацию или skill**
3. Не молчи об ошибке - предложи решение для предотвращения в будущем

**Примеры:**
- Ошибка при использовании skill → предложи обновить SKILL.md с предупреждением
- Неочевидное поведение инструмента → добавь в best-practices секцию
- Повторяющаяся проблема workflow → обнови CLAUDE.md с правилом
- Непонятный параметр API → дополни references/документацию

**Цель:** Каждая ошибка должна случиться только один раз. Workspace должен становиться умнее после каждой проблемы.

---

### Общие напоминания

- Всегда анализируй доступные skills и команды перед выполнением задачи
- Твое представление о текущей дате может быть неточным - не полагайся на него для критичных решений
- После создания/изменения файлов фронтенд-приложений ОБЯЗАТЕЛЬНО перезапускай Docker контейнеры

---

## Skills - Специализированные возможности

Все skills находятся в `.claude/skills/`. Используй их автоматически при соответствующих запросах.

### Визуализация и веб-разработка

**`dashboard-pages-generator`** 📊
- **Когда:** визуализация данных, отчеты, графики, дашборды
- **Триггеры:** "создай дашборд", "построй график", "визуализируй данные"
- **Результат:** Next.js страница с Recharts → https://my-jarvis.ru/dashboard/[name]
- **Проект:** `/opt/ai-workspace/apps/dashboard-pages/`
- ⚠️ **КРИТИЧНО:** После создания выполни `cd /opt/ai-workspace/apps/dashboard-pages && docker-compose restart`

**`website-pages-generator`** 🌐
- **Когда:** сайты, лендинги, презентационные страницы с анимациями
- **Триггеры:** "создай сайт", "сделай лендинг", "красивый сайт"
- **Результат:** Next.js страница с компонентами из @animate-ui/@aceternity → https://my-jarvis.ru/site/[name]
- **Проект:** `/opt/ai-workspace/apps/webapp/`
- **Важно:** ТОЛЬКО готовые компоненты из MCP shadcn (запрещено писать анимации с нуля)
- ⚠️ **КРИТИЧНО:** После создания выполни `cd /opt/ai-workspace/apps/webapp && docker-compose restart`

**Правило разделения:**
- **ДАННЫЕ + ГРАФИКИ** = `dashboard-pages-generator` (порт 3001, Recharts)
- **ДИЗАЙН + АНИМАЦИИ** = `website-pages-generator` (порт 3005, @animate-ui/@aceternity)

### Генерация контента

**`nano-banana-image-gen`** 🎨
- **Когда:** генерация изображений по текстовому описанию
- **Триггеры:** "создай изображение", "сгенерируй картинку", "нарисуй"
- **Возможности:**
  - Text-to-Image (генерация с нуля)
  - Image-to-Image (редактирование существующих)
  - Референсные изображения (до 14 штук)
  - Multi-turn редактирование
- **Модель:** см. `scripts/config.py` в skill
- **Методология:** "5 столпов" (Reasoning Engine, сценарии вместо тегов)
- **Качество:** 1K, 2K, 4K разрешения
- **Проект:** `.claude/skills/nano-banana-image-gen/`
- **Важно:** Используй методологию "Пять столпов" - читай best practices перед генерацией
- **Brands:** Автоматически применяет guidelines из `/opt/ai-workspace/assets/brands/[бренд]/`

**`pdf`** 📄
- **Когда:** создание PDF документов и отчетов
- **Триггеры:** "создай PDF", "сгенерируй отчет PDF", "сделай документ"
- **Возможности:**
  - Генерация профессиональных PDF
  - Поддержка кириллицы (шрифт Manrope)
  - Таблицы, графики, изображения
  - Брендинг из brands storage
- **Проект:** `.claude/skills/pdf/`

**`contract-filler`** 📝
- **Когда:** заполнение шаблонов договоров и контрактов
- **Проект:** `.claude/skills/contract-filler/`

### Работа с данными

**`analiticalo-db-analytics`** 💾
- **Когда:** запросы к CRM базе данных клиники
- **Триггеры:** "покажи клиентов", "конверсия", "выручка", "метрики"
- **MCP tool:** `mcp__buisness-database-analiticalo__query`
- **Документация:** `.claude/skills/analiticalo-db-analytics/references/`

**`xlsx`** 📑
- **Когда:** создание/редактирование Excel файлов
- **Триггеры:** "создай таблицу Excel", "экспорт в xlsx", "отчет в Excel"
- **Возможности:** формулы, стилизация, условное форматирование

### UI и компоненты

**`ui-components-library`** 🧩
- **Когда:** подбор готовых UI компонентов из shadcn
- **Используется:** при разработке интерфейсов для определения подходящих компонентов

### Коммуникация

**`telegram-notifier`** 📱
- **Когда:** отправка уведомлений/файлов в Telegram
- **Триггеры:** "отправь в телеграм", "notify telegram"
- **Команды:** `/tg [текст]`, `/tg-file [путь]`, `/tg-status`
- **Uploads:** `.claude/skills/telegram-notifier/uploads/` (автоочистка через 7 дней)
- **⚠️ Два режима работы:**
  - **Консоль Claude Code:** используй skill `telegram-notifier` → просто отправляет сообщение
  - **Telegram сессия** (видишь `telegram_agent_context.md`): используй `/update` (промежуточное) и `/tg` (финальное, **завершает процесс**)

### Управление проектом

**`skill-creator`** 🛠️
- **Когда:** создание/обновление skills
- **Использование:** автоматически при разработке новых skills

### Интеграции с внешними системами

**`bitrix24-manager`** 🏢
- **Когда:** работа с Bitrix24 (задачи, календарь, чаты, уведомления)
- **Триггеры:** "создай задачу в Битрикс", "отправь в чат задачи", "создай встречу", "уведомь в Битрикс"
- **Возможности:**
  - ✅ **Задачи:** CRUD, чаты задач (новый API с ноября 2024), комментарии, чеклисты
  - ✅ **Календарь:** события, встречи, участники
  - ✅ **Мессенджер:** уведомления, сообщения в чаты, файлы
- **Проект:** `.claude/skills/bitrix24-manager/`
- **Настройка:** Требует webhook URL в `.env` (см. `WEBHOOK_SETUP.md`)
- **Права (scopes):** task, calendar, im, disk, user
- **MCP поддержка:** Использует `mcp__b24-dev-mcp` для документации API
- **⚠️ Важно:** MCP `bitrix24-portal` ограничен - используй **skill** для полного функционала

### MCP серверы

**Bitrix24 MCP** 📖
- **MCP серверы:** `b24-dev-mcp` (документация), `bitrix24-portal` (ограниченный API)
- **Использование:** Только для получения документации API через `mcp__b24-dev-mcp__bitrix-*`
- **⚠️ Для работы с Bitrix24 используй skill `bitrix24-manager`, а не MCP!**

**Context7** 📚
- **MCP сервер:** `context7`
- **Возможности:**
  - Актуальная документация по любым библиотекам/фреймворкам
  - Примеры кода из официальных источников
- **Tools:** `mcp__context7__resolve-library-id`, `mcp__context7__query-docs`
- **Когда использовать:** нужна документация по библиотеке (React, Next.js, MongoDB и т.д.)
- **Важно:** Сначала resolve-library-id, потом query-docs

**shadcn** 🧩
- **MCP сервер:** `shadcn`
- **Возможности:**
  - Поиск готовых UI компонентов из shadcn/ui
  - Доступ к registries: @shadcn, @animate-ui, @aceternity
  - Примеры использования компонентов
- **Tools:** `mcp__shadcn__search_items_in_registries`, `mcp__shadcn__get_item_examples_from_registries`
- **Когда использовать:** разработка UI (website-pages, dashboard-pages)
- **Важно:** ВСЕГДА используй готовые компоненты вместо написания с нуля

**mcp-integration** 🔌
- **Skill для интеграции новых MCP серверов**
- **Проект:** `.claude/skills/mcp-integration/`

---

## Slash Commands

Доступные команды в `.claude/commands/`:

- `/tg [текст]` - отправить сообщение в Telegram
- `/tg-file [путь]` - отправить файл в Telegram
- `/tg-status` - проверить статус Telegram бота

---

## Типичные рабочие процессы

### Workflow 1: Создание дашборда с данными

```
Запрос → analiticalo-db-analytics (получить данные)
      → dashboard-pages-generator (создать визуализацию)
      → docker-compose restart (ОБЯЗАТЕЛЬНО!)
      → вернуть ссылку https://my-jarvis.ru/dashboard/[name]
```

### Workflow 2: Создание сайта с анимациями

```
Запрос → website-pages-generator:
         1. Найти компоненты через MCP shadcn
         2. Создать Next.js страницу
         3. docker-compose restart (ОБЯЗАТЕЛЬНО!)
      → вернуть ссылку https://my-jarvis.ru/site/[name]
```

### Workflow 3: Excel отчет

```
Запрос → analiticalo-db-analytics (получить данные)
      → xlsx (создать таблицу с формулами)
      → вернуть путь к файлу
```

### Workflow 4: Генерация изображения

```
Запрос: "Создай изображение космонавта у водопада для бренда X"
      → Определить бренд (если упомянут)
      → Прочитать brands/[бренд]/guidelines/ (если есть)
      → Прочитать best-practices для типа контента
      → Сформировать промпт по методологии "5 столпов"
      → nano-banana-image-gen (генерация)
      → /tg-file (отправить пользователю)
```

### Workflow 5: PDF отчет с брендингом

```
Запрос → analiticalo-db-analytics (получить данные, если нужно)
      → Определить бренд
      → Прочитать brands/[бренд]/guidelines/
      → pdf (создать PDF с брендингом, логотипами)
      → /tg-file (отправить пользователю)
```

### Workflow 6: Комплексный отчет (изображения + PDF)

```
Запрос → analiticalo-db-analytics (данные)
      → nano-banana-image-gen (создать графики/иллюстрации)
      → pdf (собрать PDF с изображениями и текстом)
      → /tg-file (отправить готовый документ)
```

---

## База данных

**Доступ:** Через MCP tool `mcp__buisness-database-analiticalo__query` (read-only)

**Основные таблицы:**
- `leads` - заявки клиентов и конверсии
- `sales` - транзакции продаж
- `read_before_start_analis` - документация и бизнес-логика

**Важно:**
- Уникальность клиентов по полю `phone`
- Логика конверсий и расчетов описана в `read_before_start_analis`
- Всегда читай бизнес-логику перед анализом: `.claude/skills/analiticalo-db-analytics/references/facts-and-business-logic.md`

## Документация

**MCP Supabase:** Используй для поиска документации о работе с Supabase (API, Auth, Storage, Edge Functions)
- Доступен через MCP tools для получения актуальной документации
- **НЕ** для доступа к базе данных - только для справки по функциям Supabase

---

## Ключевые правила

### ✅ ВСЕГДА:
- Использовать skills для соответствующих задач
- Перезапускать Docker после изменений в apps/*/frontend
- Проверять доступность ссылок перед отправкой пользователю (curl)
- Использовать готовые компоненты из MCP вместо написания с нуля
- Делать UI адаптивным (mobile-first, Tailwind breakpoints)

### ❌ НИКОГДА:
- Не создавать статические HTML или Python HTTP серверы для визуализации
- Не писать анимации/компоненты с нуля (использовать @animate-ui/@aceternity)
- Не коммитить .env файлы
- Не пропускать перезапуск Docker (это приведет к 404)

---

## Безопасность

- Workspace: работает от `root` (для гибкости разработки)
- Production apps: должны работать от `webapp-user`
- Секреты: хранятся в `/opt/ai-workspace/.env`
- БД: доступ только read-only через MCP

---

## Domains & URLs

**Supabase:**
- API: https://api.analiticalo.ru
- Studio: https://studio.analiticalo.ru

**Доступны для деплоя:**
- app.analiticalo.ru
- dashboard.analiticalo.ru
- admin.analiticalo.ru

**Текущие приложения:**
- Dashboard Pages: https://my-jarvis.ru/dashboard/
- Website Pages: https://my-jarvis.ru/site/

---

## Brands Storage (централизованное хранилище)

**Локация:** `/opt/ai-workspace/assets/brands/`
**Доступ:** Через симлинк `brands/` в каждом skill

**Назначение:** Единое хранилище брендовых ресурсов для всех skills (nano-banana, website-pages, dashboard-pages, pdf).

**Структура:**
```
/opt/ai-workspace/assets/brands/
├── фонд/
│   ├── guidelines/          # Текстовые описания стиля
│   │   ├── colors.txt
│   │   ├── typography.txt
│   │   └── visual_style.md
│   └── assets/              # Визуальные ресурсы
│       ├── logos/
│       ├── characters/
│       ├── products/
│       └── backgrounds/
├── люкс-оптика/
├── ангелы-зрения/
└── оптика-будущего/
```

**Использование:**
- `nano-banana-image-gen` - читает guidelines, использует assets как референсы
- `pdf` - применяет логотипы и цветовую палитру из guidelines
- `website-pages` / `dashboard-pages` - брендинг интерфейсов

**Примеры:**
```
"Создай презентацию для Фонда"
→ Claude автоматически читает brands/фонд/guidelines/
→ Применяет цвета, шрифты, стиль из guidelines
→ Использует логотипы из brands/фонд/assets/logos/
```

**Текущие бренды:**
- Фонд, Люкс Оптика, Ангелы Зрения, Оптика Будущего

---

## Расширение функционала

Для добавления новых возможностей:
- **Slash commands:** `.claude/commands/[name].md`
- **Skills:** `.claude/skills/[skill-name]/` (используй `skill-creator`)
- **Shell scripts:** `commands/[script].sh`
- **Templates:** `templates/[template-name]/`
- **Brands:** `/opt/ai-workspace/assets/brands/[бренд]/` (guidelines + assets)

Подробнее см. [SKILLS_QUICK_REFERENCE.md](./.claude/SKILLS_QUICK_REFERENCE.md)

---

**Помни:** Этот файл содержит инструкции для Claude Code. Для технической информации о проекте см. [README.md](./README.md)
